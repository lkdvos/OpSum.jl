var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/common/#Shared-utilities","page":"Shared utilities","text":"Every example page begins by includeing this file. It contains no physics — only the scaffolding that the examples share:\n\nquasi-2D lattice geometry,\nreporting of bond dimensions and construction times,\nthree tiers of verification.\n\nThe file is plain Julia, so the same include works whether you are reading the rendered docs or running an example directly from a checkout.\n\nusing OpSum\nusing OpSum: irrep_mpo, irrep_mpo_tensors, mpo_terms, mpo_tensormap, islossless, instantiate,\n    Term, Terms, TermSum, opsum, lattice, spin, spin_ops, fermion_ops, couple, project,\n    matrixunit, BipartiteAlgorithm, SVDBondAlgorithm\nusing OpSum.IrrepTensorOperators: IrrepOperator\nusing TensorKit\nusing LinearAlgebra: dot, eigvals, norm","title":"Shared utilities"},{"category":"section","location":"examples/common/#Writing-down-operators","page":"Shared utilities","text":"instances(IrrepOperator, V) enumerates the on-site alphabet as (charge, n) pairs, where n indexes TensorKit's canonical block ordering. Hard-coding those ns is fragile: the ordering depends on how V was written down, so permuting the sectors of V silently permutes every letter index and you end up with a different Hamiltonian and no error.\n\nSo the examples go the other way round: write the operator down as a TensorMap and let project expand it in the alphabet. matrixunit(V, out, in) is the shorthand for outin. Both live in OpSum itself, so there is nothing to define here — see src/operators/irrepprojection.jl. Two properties matter for the pages that follow:\n\nthe expansion is a SiteOperator, so ordinary arithmetic reads the way you would write it on paper (Sᶻ = (n↑ - n↓)/2), and A[i] places the whole expansion on site i;\ncouple(A[i], B[j]) distributes over composite operands and drops letter pairs whose charges cannot fuse to the total, so nothing has to be expanded by hand. The total defaults to the unit sector — what a Hamiltonian term needs — and to names it otherwise. Under an abelian symmetry the variadic couple(A[i], B[j], C[k], …) folds a whole chain, since every intermediate charge is forced; non-abelian chains nest so each channel is named.\n\nNothing is ever converted to a dense array, which matters for fermionic sectors where convert(Array, t) is not a well-defined operation.\n\nUnder an abelian symmetry the operands of couple may be written in any site order — the stored form is site-ordered, so an out-of-order leg is inserted and the braiding phase (for fermions, the anticommutation sign) comes with it. Non-abelian coupling still needs increasing site indices, since reordering there would need F-moves. H' gives the hermitian-conjugate partner either way.\n\nTwo builders bundle the conventions that would otherwise be re-derived per page: spin_ops for the U(1)-graded (Sp, Sm, Sz) and fermion_ops for (c, cd, n). Both, like spin and matrixunit, are memoised, so they can be called inside a term loop.","title":"Writing down operators"},{"category":"section","location":"examples/common/#Quasi-2D-geometry","page":"Shared utilities","text":"An MPO lives on a chain, so a two-dimensional lattice has to be ordered. We use column-major ordering, (x, y) ↦ (x-1)*Ly + y, with x running along the cylinder and y around it. Each rung is then contiguous and every bond spans at most Ly sites — that locality is exactly what keeps the MPO bond dimension linear in the circumference Ly and independent of the length.\n\nsiteindex(x, y, Ly) = (x - 1) * Ly + mod1(y, Ly)\n\n\"\"\"\n    cylinder_bonds(Lx, Ly; periodic_y = true) -> Vector{Tuple{Int,Int}}\n\nNearest-neighbour bonds of an `Lx × Ly` lattice in column-major order, always with the smaller\nlinear index first (`couple` only accepts left-to-right site order).\n\nFor `Ly == 2` a periodic wrap would emit the single rung bond twice, doubling its coupling, so\nthe wrap is dropped and a notice is issued.\n\"\"\"\nfunction cylinder_bonds(Lx, Ly; periodic_y = true)\n    wrap = periodic_y && Ly > 2\n    if periodic_y && Ly == 2\n        @info \"cylinder_bonds: Ly == 2, dropping the periodic wrap (it would double-count the rung)\"\n    end\n    bonds = Tuple{Int, Int}[]\n    for x in 1:Lx, y in 1:Ly\n        if y < Ly || wrap\n            push!(bonds, minmax(siteindex(x, y, Ly), siteindex(x, y + 1, Ly)))\n        end\n        x < Lx && push!(bonds, minmax(siteindex(x, y, Ly), siteindex(x + 1, y, Ly)))\n    end\n    return unique!(bonds)\nend\n\nladder_bonds(Lx, Ly) = cylinder_bonds(Lx, Ly; periodic_y = false)","title":"Quasi-2D geometry"},{"category":"section","location":"examples/common/#Reporting","page":"Shared utilities","text":"Two numbers matter. The reduced bond dimension counts the symmetry-resolved indices the MPO actually carries — this is what downstream DMRG pays for. The dense-equivalent bond dimension is the qdim-weighted sum of the per-sector multiplicities: what a symmetry-agnostic MPO would need for the same operator.\n\nbonddim(secs, b) = length(secs[b])\ndensedim(secs, b) = sum(dim(c) for c in secs[b])\nmaxbonddim(secs) = maximum(b -> bonddim(secs, b), eachindex(secs))\nmaxdensedim(secs) = maximum(b -> densedim(secs, b), eachindex(secs))\n\n\"\"\"\n    build(name, H; alg) -> NamedTuple\n\nConstruct the MPO and report its bond dimensions and (warm) construction time.\n\"\"\"\nfunction build(name, H::TermSum; alg = BipartiteAlgorithm(), quiet = false)\n    sites = lattice(H)\n    irrep_mpo(H, alg)   # warm up compilation so the timing is meaningful\n    stats = @timed irrep_mpo(H, alg)\n    Ws, secs = stats.value\n    if !quiet\n        println(\n            rpad(name, 28),\n            \" N=\", lpad(length(sites), 4),\n            \"  nterms=\", lpad(length(H), 6),\n            \"  D=\", lpad(maxbonddim(secs), 4),\n            \"  D_dense=\", lpad(maxdensedim(secs), 5),\n            \"  \", round(stats.time; digits = 4), \" s\"\n        )\n    end\n    return (; Ws, secs, D = maxbonddim(secs), Ddense = maxdensedim(secs), time = stats.time)\nend","title":"Reporting"},{"category":"section","location":"examples/common/#Verification","page":"Shared utilities","text":"Three checks of increasing cost. Every example runs at least the first.\n\n1. Faithfulness — cheap, purely symbolic, valid at any N and for any sector including fermionic ones: the reduced MPO reconstructs the original term sum exactly. Only meaningful for lossless compression; after a truncating SVD it says nothing. This is islossless, part of OpSum's public API.\n\n2. Tensor assembly — contract the assembled TensorMaps with mpo_tensormap and compare against the dense oracle. Exponential in N, so small systems only, but it stays inside TensorKit and never builds a dense array, so it is valid for fermions too.\n\nfunction mpo_matches_oracle(H::TermSum; alg = BipartiteAlgorithm())\n    sites = lattice(H)\n    Ws, secs = irrep_mpo(H, alg)\n    return mpo_tensormap(irrep_mpo_tensors(Ws, secs, sites)) ≈ instantiate(H)\nend\n\n3. Spectrum — the physics check, and the only honest route for fermions when comparing against an external reference. Eigenvalues are computed block by block, which is intrinsic and basis-independent.\n\nEach block is labelled by a coupled sector c and holds one eigenvalue per multiplet. In the full (dense) Hilbert space every such eigenvalue appears dim(c) times — for SU(2) that is the 2j+1 states of the multiplet. Repeating them accordingly is what makes spectra comparable across different symmetry groups.\n\nfunction spectrum(O::AbstractTensorMap)\n    Oop = numind(O) == 2 * numout(O) ? O : removeunit(O, numind(O))\n    vals = Float64[]\n    for (c, b) in blocks(Oop)\n        ev = real(eigvals(Matrix(b)))\n        for _ in 1:dim(c)\n            append!(vals, ev)\n        end\n    end\n    return sort!(vals)\nend\nspectrum(H::TermSum) = spectrum(instantiate(H))\n\n\"Relative deviation from Hermiticity — the sharpest detector of a wrong fermionic sign.\"\nfunction hermiticity_error(H::TermSum)\n    O = instantiate(H)\n    Oop = numind(O) == 2 * numout(O) ? O : removeunit(O, numind(O))\n    return norm(Oop - Oop') / norm(Oop)\nend\n\n\n\nThis page was generated using Literate.jl.","title":"Verification"},{"category":"section","location":"examples/long_range/#Long-range-interactions","page":"Long-range interactions","text":"Every model on the previous pages had a bond dimension independent of system size, because only a bounded number of couplings could straddle any cut. Long-range models break that: when every pair of sites interacts, the number of open channels at a bond grows with the system.\n\nThis page builds the Haldane–Shastry chain and a general power law, and shows that the exact bond dimension grows linearly:\n\nD_mathrmdense = tfrac32 N + 2 \n\nusing OpSum: OpSum\ninclude(joinpath(pkgdir(OpSum), \"examples\", \"common.jl\"))","title":"Long-range interactions"},{"category":"section","location":"examples/long_range/#Haldane–Shastry","page":"Long-range interactions","text":"H = J fracpi^2N^2 sum_n  m fracvecS_n cdot vecS_msin^2left(pi (n-m)Nright)\n\nAn all-to-all model on N sites has binomN2 terms, so this is where symbolic accumulation is felt. opsum takes the whole generator in one pass, which is Theta(M) in the number of terms — at N = 256 that is 32640 terms in a few hundredths of a second. Folding H = H + term instead copies the accumulated list on every step and so is quadratic; it is fine for a handful of terms and the wrong choice here.\n\nV = SU2Space(1 // 2 => 1)\nS = spin(V)\n\nfunction haldane_shastry(N; J = 1.0)\n    pref = J * π^2 / N^2\n    return opsum(\n        fill(V, N),\n        (\n            (pref / sin(π * (m - n) / N)^2) * dot(S[n], S[m])\n                for n in 1:(N - 1) for m in (n + 1):N\n        )\n    )\nend\n\nN = 16\nH_hs = haldane_shastry(N)\nres_hs = build(\"Haldane-Shastry\", H_hs)\n\nEven with every pair coupled, the compression is still exact:\n\nislossless(H_hs)","title":"Haldane–Shastry"},{"category":"section","location":"examples/long_range/#Linear-growth","page":"Long-range interactions","text":"The coefficient matrix of a generic long-range model has full rank, so at a cut after site b the minimum vertex cover has to keep one open spin-1 channel for roughly every site on the smaller side — giving min(b N-b) multiplets, maximised at the middle of the chain.\n\nfor L in (10, 20, 40, 60, 80)\n    r = build(\"HS N=$L\", haldane_shastry(L); quiet = true)\n    println(\n        \"  N=$(rpad(L, 3))  nterms=$(rpad(L * (L - 1) ÷ 2, 5))  D=$(rpad(r.D, 4))\",\n        \"  D_dense=$(rpad(r.Ddense, 5))  3N/2+2 = $(3L ÷ 2 + 2)\"\n    )\nend\n\nContrast that with the finite-range models: this is the one family whose MPO genuinely grows with the system, and it is why long-range Hamiltonians are the interesting stress test for MPO construction.\n\nall(\n    build(\"hs\", haldane_shastry(L); quiet = true).Ddense == 3L ÷ 2 + 2\n        for L in (10, 20, 30, 40)\n)","title":"Linear growth"},{"category":"section","location":"examples/long_range/#A-general-power-law","page":"Long-range interactions","text":"H = J sum_nm fracvecS_n cdot vecS_mn-m^alpha\n\nThe exponent controls how fast the couplings decay, but not the exact bond dimension: any coefficient matrix of full rank gives the same linear law. Truncation is what exploits the decay.\n\nfunction powerlaw(N; α = 3.0, J = 1.0)\n    return opsum(\n        fill(V, N),\n        (\n            (J * abs(m - n)^(-α)) * dot(S[n], S[m])\n                for n in 1:(N - 1) for m in (n + 1):N\n        )\n    )\nend\n\nfor α in (1.0, 2.0, 3.0, 6.0)\n    r = build(\"powerlaw α=$α\", powerlaw(24; α); quiet = true)\n    println(\"  α=$(rpad(α, 4))  D=$(rpad(r.D, 4))  D_dense=$(r.Ddense)\")\nend","title":"A general power law"},{"category":"section","location":"examples/long_range/#Trading-exactness-for-size","page":"Long-range interactions","text":"BipartiteAlgorithm (the default) is exact. SVDBondAlgorithm instead compresses each bond by a truncated SVD across all charge sectors at once, so a fast-decaying power law can be squeezed hard for a controlled error.\n\nNote that faithfulness via mpo_terms is meaningless once the compression is lossy — it assumes an intact identity backbone. The honest check is the operator error against the dense oracle, so we measure that at a size where the oracle is affordable.\n\nusing MatrixAlgebraKit: truncrank\n\nlet H = powerlaw(6; α = 3.0), sites_c = lattice(H)\n    oracle = instantiate(H)\n    exact = build(\"powerlaw exact\", H; quiet = true)\n    println(\"  exact:            D_dense=$(exact.Ddense)   rel. error 0\")\n    for k in (8, 6, 4, 2, 1)\n        Ws, secs = irrep_mpo(H, SVDBondAlgorithm(truncrank(k)))\n        O = mpo_tensormap(irrep_mpo_tensors(Ws, secs, sites_c))\n        err = norm(O - oracle) / norm(oracle)\n        D = maximum(b -> sum(dim(c) for c in secs[b]), eachindex(secs))\n        println(\"  truncrank($(rpad(k, 2)))     D_dense=$(rpad(D, 4))  rel. error $(round(err; sigdigits = 3))\")\n    end\nend","title":"Trading exactness for size"},{"category":"section","location":"examples/long_range/#Scaling","page":"Long-range interactions","text":"julia --project=benchmark scripts/plot_benchmarks.jl --run --sweep full\n\nThe linear bond dimension has a price in construction time. Every in-flight coupling is one edge of the bipartite graph at every bond it straddles, so the exact sweep costs sum_mathrmterms mathrmspan: linear in N for a finite-range model, but O(N^3) when all binomN2 pairs interact. That is intrinsic rather than an implementation artefact — at a cut after site b the coefficient matrix J(nm) restricted to n le b  m is genuinely dense, so the edges have to be there. Truncation, above, is the lever for large long-range systems.\n\n(Image: Bond dimension and construction time versus system size)\n\nThe profile of the bond dimension along the chain makes the contrast with the local models vivid: long-range couplings peak at the middle of the chain, where the cut separates the largest number of interacting pairs — min(b N-b) open channels, a triangle in b — while finite-range and quasi-2D models sit on a flat plateau. (The figure is log-scaled in D_mathrmdense so the plateaus, two orders of magnitude below the peak, stay legible.)\n\n(Image: Bond dimension profile along the chain)\n\n\n\nThis page was generated using Literate.jl.","title":"Scaling"},{"category":"section","location":"operators/#Building-operators","page":"Building operators","text":"This page is the guide to the operator interface: how to get a Hamiltonian into OpSum, and the patterns that keep it correct. The example pages — starting with Spin chains — work the same material through concrete models; Reference lists every docstring.","title":"Building operators"},{"category":"section","location":"operators/#The-shape-of-the-pipeline","page":"Building operators","text":"TensorMap ──project──► SiteOperator ──A[i]──► TermSum ──irrep_mpo──► (Ws, bondsectors)\n(what you                (on-site,    (placed and                    │\n write down)              unplaced)    coupled)                      ▼\n                                          │              irrep_mpo_tensors\n                                          └──instantiate──► TensorMap (dense oracle)\n\nThree types carry the whole interface:\n\nType What it is How you get one\nSiteOperator an operator on one site, not yet placed project, matrixunit, spin, spin_ops, fermion_ops, scalarop\nTerms a bag of Terms, not yet on a lattice A[i], couple, dot, project, +, *\nTermSum those terms plus the lattice — the compressible Hamiltonian opsum, H', +, append!\n(Ws, bondsectors) the reduced MPO irrep_mpo\n\nEverything named above is exported, so using OpSum is enough — you do not need a using OpSum: … list. The full surface is IrrepOperator, spin, spin_ops, fermion_ops, scalarop, project, matrixunit, islossless, mpo_tensormap, Term, Terms, TermSum, couple, opsum, lattice, canonicalize!, irrep_mpo, irrep_mpo_tensors, jordan_mpo_tensors, mpo_terms, instantiate, BipartiteAlgorithm, SVDBondAlgorithm and the bond-basis strategies VertexCover, IndependentSVD, SequentialSVD.\n\nNote that dot (for the Cartesian Sᵢ·Sⱼ) is LinearAlgebra.dot, so that one still needs using LinearAlgebra: dot.","title":"The shape of the pipeline"},{"category":"section","location":"operators/#Never-name-an-alphabet-letter-by-its-index","page":"Building operators","text":"The on-site alphabet is the set of irreducible tensor operators IrrepOperator{I}(c, n): an operator charge c and a canonical index n. You can enumerate it:\n\nusing OpSum, TensorKit\nusing OpSum.IrrepTensorOperators: IrrepOperator\n\nV = Rep[U₁](0 => 1, 1 => 1)\ninstances(IrrepOperator, V)\n\nDo not write those ns into your code. n indexes TensorKit's canonical block ordering, which depends on how V was spelled. Reorder the sectors of V and every letter index permutes silently — you get a different Hamiltonian and no error anywhere.\n\nWrite the operator down instead, and let OpSum find the letters.","title":"Never name an alphabet letter by its index"},{"category":"section","location":"operators/#On-site-operators","page":"Building operators","text":"","title":"On-site operators"},{"category":"section","location":"operators/#Abelian-and-fermionic:-matrixunit","page":"Building operators","text":"For spaces whose sectors are one-dimensional with multiplicity one — Vect[U₁], Vect[FermionNumber], products thereof — name the matrix unit outin by its sectors:\n\ndn, up = U1Irrep(0), U1Irrep(1)\n\nSp = matrixunit(V, up, dn)                                  # S⁺\nSm = matrixunit(V, dn, up)                                  # S⁻\nSz = (matrixunit(V, up, up) - matrixunit(V, dn, dn)) / 2     # Sᶻ\n\nAn SiteOperator supports ordinary arithmetic (+ - * /), so composite operators read the way you would write them on paper. Sᶻ here is genuinely composite — two letters:\n\nlength(Sz)\n\nThose two sets are common enough to have builders: spin_ops returns (; Sp, Sm, Sz) for a U(1)-graded spin-s site and fermion_ops returns (; c, cd, n) for a fermionic mode. spin_ops takes the sectors in descending magnetic quantum number, because the labels cannot say which is which — Rep[U₁](0 => 1, 1 => 1) above is labelled by particle number, not by m.\n\nspin_ops(V, up, dn).Sz ≈ Sz","title":"Abelian and fermionic: matrixunit"},{"category":"section","location":"operators/#SU(2):-spin","page":"Building operators","text":"With SU(2) symmetry there is one on-site operator to speak of, the rank-1 vector operator vecS, normalized so that dot reproduces the Cartesian scalar product:\n\nVsu2 = SU2Space(1//2 => 1)\nS = spin(Vsu2)","title":"SU(2): spin"},{"category":"section","location":"operators/#Anything-else:-project","page":"Building operators","text":"For any other single-site operator, build the TensorMap and project it. Both a plain V ← V and a charged V ← V ⊗ Vect[I](c => 1) are accepted:\n\nusing LinearAlgebra: norm\n\nVf = Vect[FermionNumber](0 => 1, 1 => 1)\nvac, occ = FermionNumber(0), FermionNumber(1)\n\nnhat = matrixunit(Vf, occ, occ)                    # equivalently:\nnhat2 = project(OpSum.instantiate(nhat, Vf), Vf)   # project the dense form back\nnorm(OpSum.instantiate(nhat, Vf) - OpSum.instantiate(nhat2, Vf))\n\nNothing is ever converted to a dense Array, which matters for fermionic sectors where convert(Array, t) is not well defined.\n\nThe identity is not an alphabet letter. project(id(V), V) returns a sum of trivial-charge letters, and a scalar multiple of the identity is scalarop.","title":"Anything else: project"},{"category":"section","location":"operators/#Placing-and-coupling","page":"Building operators","text":"A[i] places an on-site operator on site i, distributing over its letters, and gives a one-term TermSum. couple(a, b; to = c) fuses two of them into a two-site term with total charge c:\n\nterm = couple(Sz[1], Sz[2])\n\nFour rules govern couple:\n\nSite order is free under an abelian symmetry, increasing otherwise. A term is stored in site order, so an operand acting to the left of an earlier one has its leg inserted rather than appended, and the braiding phase that costs comes with it. Under a UniqueFusion symmetry — which includes every fermionic sector — couple does that itself, so c^_i+1 c_i is written as it reads and comes out as -c_i c^_i+1. Under a non-abelian symmetry reordering would need F-moves, so each operand must act strictly to the right of the ones before it. (dot accepts either order for any symmetry: two legs coupling to the unit sector need no F-move.)\nComposite operands are fine. Every side may have several letters; the coupling distributes over every combination, and combinations whose charges cannot fuse to to are dropped. It is an error if none fuse.\nto is the total charge of the term, and defaults to the unit sector. A term in a Hamiltonian is a scalar, so the default is what you almost always want. Pass to explicitly to build a charged object — those are legal and useful as building blocks. If the charges cannot reach to you get an error rather than a silently empty result, which is what makes the default safe: couple(Sp[1], Sp[2]) throws instead of quietly vanishing.\nThree or more sites: abelian folds, non-abelian nests. Under an abelian symmetry (UniqueFusion — U₁, ℤₙ, FermionNumber, Trivial, products thereof) every intermediate charge is forced, so there is nothing to choose and the variadic form does the whole chain:\n\nusing OpSum: total\n\nVf = Vect[FermionNumber](0 => 1, 1 => 1)\ncop = matrixunit(Vf, FermionNumber(0), FermionNumber(1))\ncdag = matrixunit(Vf, FermionNumber(1), FermionNumber(0))\n\nH4 = couple(cdag[1], cop[2], cdag[3], cop[4])       # charge-neutral four-fermion term\ntotal(only(H4))\n\nUnder a non-abelian symmetry the intermediates are real freedom, so the variadic form throws and you nest, naming each channel — which also means the choice reads back off the term:\n\nchirality = couple(couple(S[1], S[2]; to = SU2Irrep(1)), S[3]; to = SU2Irrep(0))\n\nFor the SU(2) scalar product vecS_i cdot vecS_j use dot, which is couple(...; to = unit(I)) times the Cartesian factor -sqrtdim c:\n\nusing LinearAlgebra: dot\n\nheisenberg(N; J = 1.0) = opsum(fill(Vsu2, N), (J * dot(S[i], S[i + 1]) for i in 1:(N - 1)))\nH = heisenberg(6)\n\ndot does not distribute over composite operands — its factor is per-letter, so it has no meaning for an operator mixing charges. Use couple for those.","title":"Placing and coupling"},{"category":"section","location":"operators/#The-hermitian-conjugate-partner","page":"Building operators","text":"H' (adjoint) builds it, so a hopping amplitude only has to be written once:\n\nT = opsum(fill(Vf, 2), -1.0 * couple(cdag[1], cop[2]))\nT + T' ≈ opsum(fill(Vf, 2), -1.0 * (couple(cdag[1], cop[2]) + couple(cdag[2], cop[1])))\n\nIt is defined on a TermSum and not on a Terms bag, because it needs the physical spaces — the adjoint of an alphabet letter is generally a combination of the dual charge's letters, which only the space knows. Every term must be charge-neutral: a charged term's adjoint lives in the dual sector.","title":"The hermitian-conjugate partner"},{"category":"section","location":"operators/#Projecting-a-whole-block","page":"Building operators","text":"If you already have a K-site operator as a TensorMap — from a paper, an ED code, a kron — hand it to project directly. It need not factorize into A_i B_j; a generic block works:\n\nhbond = OpSum.instantiate(\n    (1 / 2) * couple(Sp[1], Sm[2]) +\n        (1 / 2) * couple(Sm[1], Sp[2]) +\n        couple(Sz[1], Sz[2]),\n    [V, V],\n)\n\nsites = fill(V, 6)\nHxxz = opsum(sites, (project(hbond, [i, i + 1]) for i in 1:5))\nlength(Hxxz)\n\nTwo accepted shapes, with K = length(sites) = numout(h):\n\nh : V_1 ⊗ … ⊗ V_K  ←  V_1 ⊗ … ⊗ V_K                        # charge-neutral\nh : V_1 ⊗ … ⊗ V_K  ←  V_1 ⊗ … ⊗ V_K ⊗ Vect[I](tot => 1)    # total charge tot\n\nThe physical spaces are read off h; sites supplies only the labels, and must be strictly increasing. The coefficients are exact inner products against a complete orthogonal basis, so this is an expansion, not a fit — and project re-materializes its own output and compares it against the input, throwing if the result is not faithful. atol/rtol control what counts as negligible.\n\nwarning: Projected terms have full support\nEvery returned term is active on all K sites. An on-site identity factor comes back as a trivial-charge letter, not as a shorter term. So project inverts instantiate only for operators whose terms all have full support on sites — projecting vecS_1 cdot vecS_2 + tfrac14 gives two two-site terms, not a two-site term plus a constant. The MPO is still correct; it may just carry a channel you would have written more compactly by hand.","title":"Projecting a whole block"},{"category":"section","location":"operators/#Building-the-MPO","page":"Building operators","text":"using OpSum: irrep_mpo, irrep_mpo_tensors, mpo_terms\n\nWs, secs = irrep_mpo(Hxxz)\nmap(length, secs)\n\nWs[i] is the sparse bond matrix at site i and secs[i] lists the charge of each bond index to its right, so length(secs[i]) is the reduced bond dimension and sum(dim, secs[i]) the dense-equivalent one. irrep_mpo_tensors(Ws, secs, sites) assembles symmetric TensorMaps in the MPSKit leg convention B_i-1 otimes V_i leftarrow V_i otimes B_i.\n\nThe second argument selects the bond algorithm: BipartiteAlgorithm() (default, lossless minimum vertex cover) or SVDBondAlgorithm(trunc) with a MatrixAlgebraKit truncation strategy.","title":"Building the MPO"},{"category":"section","location":"operators/#The-lattice-travels-with-the-operator","page":"Building operators","text":"Note that irrep_mpo took no sites: a TermSum already carries the lattice, because opsum attached it. That is deliberate — instantiate and irrep_mpo_tensors need the space of every site, idle ones included, which no individual term can know, so there is no useful state in which an operator is compressible but latticeless.\n\nopsum is also the only place where the operators and the spaces you are compressing them with are ever confronted: every letter is checked against the space of the site it sits on, and every term against 1:length(sites).\n\nlattice(Hxxz) == sites\n\nPlacement, coupling and project all work without a lattice — they return a Terms bag — so the natural shape for a model builder is to end in opsum:\n\nheisenberg(N; J = 1.0) = opsum(fill(Vsu2, N), (J * dot(S[i], S[i + 1]) for i in 1:(N - 1)))\n\nSVDBondAlgorithm has two truncation semantics, chosen by its sweep keyword. They agree exactly when trunc === nothing, and differ only once truncation bites:\n\nsweep meaning of truncrank(k)\nIndependentSVD (default) k retained indices per bond — every bond is compressed on the raw prefix/suffix classes, independently of its neighbours\nSequentialSVD k retained indices after upstream truncation — each bond is compressed in the basis the previous bond left behind, so an aggressive early truncation can starve the downstream bonds\n\nirrep_mpo(H, SVDBondAlgorithm(truncrank(8)))                        # per-bond\nirrep_mpo(H, SVDBondAlgorithm(truncrank(8); sweep = SequentialSVD)) # left-to-right sweep","title":"The lattice travels with the operator"},{"category":"section","location":"operators/#Jordan-form,-for-an-MPO-library","page":"Building operators","text":"irrep_mpo_tensors gives one dense TensorMap per site, with the whole bond collapsed into a single GradedSpace — the right shape for contracting a small chain and checking it, the wrong one for an MPO library, which wants the block structure back. jordan_mpo_tensors emits that instead: one BlockTensorKit.SparseBlockTensorMap per site, one level per bond index, and the bond indices reordered into upper-triangular (Jordan) form\n\n⎛ 1  C  D ⎞     row/column 1   = \"nothing placed yet\"\n⎜ ·  A  B ⎟     row/column end = \"everything placed\"\n⎝ ·  ·  1 ⎠\n\nWsj = jordan_mpo_tensors(Hxxz)\nmap(W -> size(W, 4), Wsj)\n\nThis is what MPSKit's JordanMPOTensor and FiniteMPOHamiltonian consume; OpSum itself does not depend on MPSKit. Since every bond space is already known exactly, the consumer needs no space-deduction fixed point.\n\nBoth identity channels are emitted at every internal bond, even where the compression did not spend a bond index on them: a chain with no on-site fields has nothing starting to the right of the last bonds, so the minimum vertex cover legitimately omits the start channel there, and nothing has finished yet at the first bonds. The emitted MPO is therefore minimal among Jordan-form MPOs and can exceed irrep_mpo's unconstrained minimum by at most two indices per bond — in practice +1 at the first internal bond and +1 at the last, 0 in the bulk. The padded channels are pure identity chains that cost no dense storage and cannot change the operator.\n\nThe total charge must be trivial (a Jordan MPO's right boundary is an identity), and a truncation aggressive enough to empty a bond is rejected rather than silently emitted.","title":"Jordan form, for an MPO library"},{"category":"section","location":"operators/#Recommended-patterns","page":"Building operators","text":"Write the operator, not the index. Anything that mentions a bare IrrepOperator(c, n) in model code is a latent bug. Go through matrixunit, spin or project.\n\nBuild local operators wherever reads best. spin, matrixunit, spin_ops and fermion_ops are memoised per space and sectors, so calling them inside the term loop costs a dictionary lookup. (This used to be \"hoist them out\"; matrixunit runs a whole projection, which is now paid once.)\n\nHand every term to opsum at once. It makes one pass over each argument, so building an M-term Hamiltonian is Theta(M) — an all-to-all model on 256 sites (32640 terms) assembles in a few hundredths of a second. Any number of arguments is allowed, and iterables nest, so several families of terms need no vcat:\n\nSop = spin(Vphys)\nH = opsum(\n    fill(Vphys, N),\n    (J1 * dot(Sop[i], Sop[j]) for (i, j) in nn_bonds),\n    (J2 * dot(Sop[i], Sop[j]) for (i, j) in nnn_bonds),\n)\n\nH + term and append!(H, terms) also work, and append! is likewise one pass. But + copies the accumulated list, so folding it — for t in terms; H = H + t; end — is quadratic. That is fine for a handful of terms and the wrong choice for thousands.\n\nThe normal form (coincident terms summed, cancelled ones dropped) is not taken on the way in: it happens once, in canonicalize!, when the term set is first observed or compressed. So length(H) always reports the number of terms the operator has, never the number appended.\n\nBuild a bond block once, project per bond. project is cheap for local blocks (well under a millisecond for a two-site spin-½ operator), so projecting the same tensor on each bond of a translation-invariant chain costs nothing worth optimizing. Build the TensorMap once outside the loop.\n\nOrder the sites of a 2D lattice so bonds stay short. An MPO lives on a chain. Column-major ordering (x y) mapsto (x-1)L_y + y keeps every bond of a cylinder within L_y sites, which is what makes the bond dimension linear in the circumference and independent of the length.\n\nPick the largest symmetry you can express. The same Heisenberg chain needs 3 reduced bond indices under SU(2) and 6 under U(1). The reduced number is what a DMRG sweep pays for.","title":"Recommended patterns"},{"category":"section","location":"operators/#Verifying-an-operator","page":"Building operators","text":"Three checks, in increasing cost:\n\n# 1. Faithfulness — cheap, symbolic, valid at any N and for any sector including fermionic.\n#    `islossless` does the reconstruct-and-compare in one call: `≈` on two term sums compares the\n#    canonical term sets exactly and the coefficients approximately.\nislossless(Hxxz)\n\n# 2. Tensor assembly — contract the MPO with `mpo_tensormap` and compare against the dense oracle.\n#    Exponential in N, but it stays inside TensorKit, so it is valid for fermions too.\nmpo_tensormap(irrep_mpo_tensors(Ws, secs, sites)) ≈ OpSum.instantiate(Hxxz)\n\nSpectrum — the physics check, and the only honest route for fermions against an external reference. Diagonalize OpSum.instantiate(H) block by block, repeating each eigenvalue dim(c) times so spectra are comparable across symmetry groups. examples/common.jl has a spectrum helper; a hermiticity_error check is the sharpest detector of a wrong fermionic sign.\n\nNote that check 1 says nothing after a truncating SVDBondAlgorithm — it assumes lossless compression.","title":"Verifying an operator"},{"category":"section","location":"operators/#Reading-an-error","page":"Building operators","text":"Message Cause\ncouple: … left of site … reordering the legs of a SimpleFusion() coupling needs F-moves out-of-order site indices under a non-abelian symmetry, where the reordering is not available; write them increasing\ncouple: no pair of terms of the operands fuses to … the requested total charge is unreachable from the operands' charges\ncouple: every term of the second operand must be a single-site charged operator the right operand has a scalar (identity) part, or spans several sites\nproject: sites must be strictly increasing sorted-unique labels are a downstream invariant; project will not permute h for you, since that needs braiding and flips fermionic signs\nproject: unsupported operator space domain ≠ codomain, or a charge leg that is dual or has degeneracy > 1\nproject: the projected term sum is not faithful atol/rtol discarded real weight — lower them\nGenericFusion … deferred sectors with fusion multiplicity > 1 are out of scope","title":"Reading an error"},{"category":"section","location":"examples/spin_chains/#Spin-chains","page":"Spin chains","text":"This page builds the three workhorse one-dimensional spin models and shows how the symmetry you impose and the range of the interaction each leave a distinct fingerprint on the MPO bond dimension:\n\nmodel symmetry reduced D dense-equivalent D\nHeisenberg SU(2) 3 5\nXXZ U(1) 6 6\nJ_1–J_2 SU(2) 4 8\n\nThe reduced bond dimension counts symmetry-resolved indices — what a DMRG sweep actually pays for. The dense-equivalent one is what a symmetry-agnostic MPO would need for the same operator.\n\nusing OpSum: OpSum\ninclude(joinpath(pkgdir(OpSum), \"examples\", \"common.jl\"))","title":"Spin chains"},{"category":"section","location":"examples/spin_chains/#SU(2)-Heisenberg","page":"Spin chains","text":"H = J sum_i vecS_i cdot vecS_i+1\n\nWith SU(2) symmetry there is a single on-site operator to speak of: the rank-1 vector operator vecS, provided by spin. dot contracts two of them into the rotationally invariant scalar product, so the Hamiltonian is a one-liner.\n\nNote the two idioms used here and throughout. S = spin(V) is hoisted out of the loop (it is memoised, so this is taste rather than necessity), and the terms are handed to opsum together with the lattice, in one pass. opsum is what turns a bag of terms into a compressible operator: it attaches the physical spaces — checking every letter against the site it sits on — so nothing downstream needs a second sites argument.\n\nV = SU2Space(1 // 2 => 1)\nS = spin(V)\n\nheisenberg(N; J = 1.0) = opsum(fill(V, N), (J * dot(S[i], S[i + 1]) for i in 1:(N - 1)))\n\nN = 8\nH_heis = heisenberg(N)\nres_heis = build(\"Heisenberg SU(2)\", H_heis)\n\nThe compression is lossless — the reduced MPO reconstructs every original term exactly:\n\nislossless(H_heis)\n\nand contracting the assembled tensors reproduces the dense operator:\n\nmpo_matches_oracle(heisenberg(4))\n\nThe bulk bond carries an identity-in channel, an identity-out channel and one open spin-1 multiplet: 1 + 1 + 3 = 5 in dense terms, but only 3 symmetry-resolved indices.\n\nres_heis.D, res_heis.Ddense","title":"SU(2) Heisenberg"},{"category":"section","location":"examples/spin_chains/#U(1)-XXZ","page":"Spin chains","text":"H = fracJ2 sum_i left( S^+_i S^-_i+1 + S^-_i S^+_i+1 right)\n    + J Delta sum_i S^z_i S^z_i+1\n\nKeeping only U(1) means naming individual raising, lowering and S^z operators. Rather than hard-coding alphabet indices, spin_ops derives them from the matrix units, which is robust against how V happens to be written down. It takes the sectors in descending m — the labels themselves cannot say which is which, since a Vect[U₁] spin site is as often labelled by particle number as by m, exactly as here.\n\nVu = Rep[U₁](0 => 1, 1 => 1)\ndn, up = U1Irrep(0), U1Irrep(1)\n\nSp, Sm, Sz = spin_ops(Vu, up, dn)                        # ``S^+``, ``S^-``, ``S^z``\n\nS^z is a genuinely composite on-site operator — a two-letter combination:\n\nlength(Sz)\n\ncouple distributes over both expansions, so a composite operand needs no special handling: the S^z S^z term is written exactly like the single-letter ones.\n\nfunction xxz(N; J = 1.0, Δ = 1.0)\n    return opsum(\n        fill(Vu, N),\n        (\n            J / 2 * couple(Sp[i], Sm[i + 1]) +\n                J / 2 * couple(Sm[i], Sp[i + 1]) +\n                J * Δ * couple(Sz[i], Sz[i + 1])\n                for i in 1:(N - 1)\n        )\n    )\nend\n\nH_xxz = xxz(N)\nres_xxz = build(\"XXZ U(1)\", H_xxz)\n\nislossless(H_xxz)\n\nAt Delta = 1 the XXZ chain is the Heisenberg chain. The two builds live on different spaces with different symmetry groups, so the sharpest available check is that they have the same spectrum:\n\nspectrum(heisenberg(6)) ≈ spectrum(xxz(6))\n\nThe same operator, but not the same MPO. The SU(2) build needs 3 symmetry-resolved indices where the U(1) build needs 6, because a single spin-1 multiplet replaces three separate abelian channels (+1, -1, 0). The reduced number is what a DMRG sweep pays for, so this factor of two is the practical benefit of imposing the larger symmetry.\n\nThe dense-equivalent sizes differ slightly too (5 versus 6). That is a property of how each Hamiltonian is written: S^z is a two-letter operator, so S^z_i S^z_j enters as four letter pairs which do not collapse into a single channel, whereas the SU(2) scalar product is one irreducible object.\n\n(su2 = (res_heis.D, res_heis.Ddense), u1 = (res_xxz.D, res_xxz.Ddense))","title":"U(1) XXZ"},{"category":"section","location":"examples/spin_chains/#Projecting-a-whole-bond-at-once","page":"Spin chains","text":"Naming the on-site factors is not the only option. If you already have the two-site bond operator as a TensorMap — from a paper's matrix, from an ED code, from kron — hand it straight to project, which expands it in the two-site ITO term basis. Nothing has to factorize into A_i B_j: a generic two-site block works, and the coefficients are exact inner products against a complete orthogonal basis, so the expansion is not a fit.\n\nh_bond = instantiate(\n    (1 / 2) * couple(Sp[1], Sm[2]) +\n        (1 / 2) * couple(Sm[1], Sp[2]) +\n        couple(Sz[1], Sz[2]),\n    [Vu, Vu],\n)\n\nH_proj = opsum(fill(Vu, N), (project(h_bond, [i, i + 1]) for i in 1:(N - 1)))\n\nproject re-materializes its own output and compares it against the input, so a faithful result is checked rather than assumed. Summed over bonds it reproduces the hand-written chain, term for term:\n\nH_proj ≈ H_xxz\n\nThe one thing to know: every projected term is active on all the sites you pass. An on-site identity factor comes back as a trivial-charge letter rather than a shorter term, so project inverts instantiate only for operators whose terms have full support on those sites.","title":"Projecting a whole bond at once"},{"category":"section","location":"examples/spin_chains/#J_1–J_2","page":"Spin chains","text":"H = J_1 sum_i vecS_i cdot vecS_i+1 + J_2 sum_i vecS_i cdot vecS_i+2\n\nAdding a next-nearest-neighbour coupling means two spin-1 channels can be open across a bond at once, so the bond dimension grows — but it is still independent of N.\n\nfunction j1j2(N; J1 = 1.0, J2 = 0.5)\n    return opsum(\n        fill(V, N),\n        (J1 * dot(S[i], S[i + 1]) for i in 1:(N - 1)),\n        (J2 * dot(S[i], S[i + 2]) for i in 1:(N - 2)),\n    )\nend\n\nH_j1j2 = j1j2(N)\nres_j1j2 = build(\"J1-J2 SU(2)\", H_j1j2)\nislossless(H_j1j2)","title":"J_1–J_2"},{"category":"section","location":"examples/spin_chains/#Bond-dimension-is-independent-of-system-size","page":"Spin chains","text":"For any finite-range interaction the bond dimension saturates: it is set by how many couplings can straddle a single cut, not by how long the chain is.\n\nfor L in (8, 16, 32, 64)\n    r = build(\"h\", heisenberg(L); quiet = true)\n    println(\"  Heisenberg  N=$(lpad(L, 3))  D=$(r.D)  D_dense=$(r.Ddense)\")\nend\n\nfor L in (8, 16, 32, 64)\n    r = build(\"j\", j1j2(L); quiet = true)\n    println(\"  J1-J2       N=$(lpad(L, 3))  D=$(r.D)  D_dense=$(r.Ddense)\")\nend","title":"Bond dimension is independent of system size"},{"category":"section","location":"examples/spin_chains/#Scaling","page":"Spin chains","text":"Construction time and bond dimension across system size, for these and the other models, are collected by the benchmark harness in benchmark/ and plotted by scripts/plot_benchmarks.jl. Reproduce the figure with\n\njulia --project=benchmark scripts/plot_benchmarks.jl --run --sweep full\n\n(Image: Bond dimension and construction time versus system size)\n\nThe time panel above is the whole pipeline: symbolic term-sum assembly plus MPO compression. Splitting the two (--figure phases) shows where the work actually goes — for a finite-range model the compression is linear in N, because each term is an open channel only on the bonds it actually straddles, so the total is dominated by the term-sum accumulation:\n\n(Image: Term-sum assembly versus MPO compression)\n\n\n\nThis page was generated using Literate.jl.","title":"Scaling"},{"category":"section","location":"examples/ladders_and_cylinders/#Ladders-and-cylinders","page":"Ladders and cylinders","text":"An MPO lives on a chain, so a two-dimensional lattice has to be flattened into one. This page builds the SU(2) Heisenberg antiferromagnet on two-leg ladders and on L_x times L_y cylinders, and measures how the bond dimension responds.\n\nThe result is the one that governs the cost of cylinder DMRG: the bond dimension is set by the circumference, not the length. For the nearest-neighbour Heisenberg cylinder it is exactly\n\nD_mathrmdense = 3 L_y + 2 \n\nindependent of L_x.\n\nusing OpSum: OpSum\ninclude(joinpath(pkgdir(OpSum), \"examples\", \"common.jl\"))","title":"Ladders and cylinders"},{"category":"section","location":"examples/ladders_and_cylinders/#Ordering-the-lattice","page":"Ladders and cylinders","text":"We use column-major ordering, (x y) mapsto (x-1) L_y + y, with x running along the cylinder and y around it. Each rung is then contiguous in the chain, and every bond spans at most L_y sites. That bounded span is precisely what keeps the bond dimension finite.\n\ncylinder_bonds emits nearest-neighbour bonds with the smaller linear index first, because couple is strictly left-to-right.\n\ncylinder_bonds(3, 3)\n\nA cylinder is periodic around y; a ladder is the same construction with open boundaries. (For L_y = 2 the periodic wrap would emit the single rung twice and silently double its coupling, so cylinder_bonds drops it and says so.)\n\nladder_bonds(3, 2)","title":"Ordering the lattice"},{"category":"section","location":"examples/ladders_and_cylinders/#The-Hamiltonian","page":"Ladders and cylinders","text":"H = J sum_langle i j rangle vecS_i cdot vecS_j\n\nsummed over the bonds of the chosen geometry. Because every bond is a plain SU(2) scalar product, the geometry is the only thing that changes between one and two dimensions.\n\nV = SU2Space(1 // 2 => 1)\nS = spin(V)\n\nfunction heisenberg_bonds(N, bonds; J = 1.0)\n    return opsum(fill(V, N), (J * dot(S[i], S[j]) for (i, j) in bonds))\nend\n\nheisenberg_cylinder(Lx, Ly; kwargs...) =\n    heisenberg_bonds(Lx * Ly, cylinder_bonds(Lx, Ly); kwargs...)\nheisenberg_ladder(Lx, Ly = 2; kwargs...) =\n    heisenberg_bonds(Lx * Ly, ladder_bonds(Lx, Ly); kwargs...)","title":"The Hamiltonian"},{"category":"section","location":"examples/ladders_and_cylinders/#A-two-leg-ladder","page":"Ladders and cylinders","text":"Lx = 6\nH_ladder = heisenberg_ladder(Lx)\nres_ladder = build(\"ladder 6x2\", H_ladder)\n\nislossless(H_ladder)\n\nThe smallest non-trivial case, a single 2 times 2 plaquette, is small enough to check against the dense operator directly:\n\nmpo_matches_oracle(heisenberg_cylinder(2, 2))","title":"A two-leg ladder"},{"category":"section","location":"examples/ladders_and_cylinders/#Cylinders:-growth-in-the-circumference","page":"Ladders and cylinders","text":"Sweeping the circumference at fixed length shows a clean linear law. Each extra ring of the cylinder adds one more leg bond that can straddle a cut, and each open bond carries a spin-1 multiplet of quantum dimension 3 — hence 3 L_y + 2, the +2 being the identity-in and identity-out channels.\n\nfor Ly in 3:6\n    r = build(\"cylinder 4x$Ly\", heisenberg_cylinder(4, Ly); quiet = true)\n    println(\"  Ly=$Ly   D=$(rpad(r.D, 3))  D_dense=$(rpad(r.Ddense, 4))  3Ly+2 = $(3Ly + 2)\")\nend","title":"Cylinders: growth in the circumference"},{"category":"section","location":"examples/ladders_and_cylinders/#...-and-independence-from-the-length","page":"Ladders and cylinders","text":"Stretching the cylinder at fixed circumference changes nothing. This is the whole reason cylinder DMRG is feasible: cost is exponential in circumference but only linear in length.\n\nfor Ly in (3, 4)\n    for Lx in (3, 4, 6, 8)\n        r = build(\"cyl\", heisenberg_cylinder(Lx, Ly); quiet = true)\n        println(\"  Ly=$Ly  Lx=$(rpad(Lx, 2))  N=$(rpad(Lx * Ly, 3))  D_dense=$(r.Ddense)\")\n    end\nend\n\nStated as an assertion over the whole grid:\n\nall(\n    build(\"c\", heisenberg_cylinder(Lx, Ly); quiet = true).Ddense == 3Ly + 2\n        for Ly in 3:6, Lx in (3, 4, 5)\n)","title":"... and independence from the length"},{"category":"section","location":"examples/ladders_and_cylinders/#Symmetry-reduction","page":"Ladders and cylinders","text":"The reduced bond dimension — the number of symmetry-resolved indices, and the quantity a DMRG sweep actually pays for — is L_y + 2: one index per open spin-1 multiplet plus the two identity channels. Imposing SU(2) therefore shrinks the bond by roughly a factor of three relative to a symmetry-agnostic MPO of the same operator.\n\nmap(3:6) do Ly\n    r = build(\"c\", heisenberg_cylinder(4, Ly); quiet = true)\n    return (Ly = Ly, reduced = r.D, dense = r.Ddense, ratio = round(r.Ddense / r.D; digits = 2))\nend","title":"Symmetry reduction"},{"category":"section","location":"examples/ladders_and_cylinders/#Scaling","page":"Ladders and cylinders","text":"julia --project=benchmark scripts/plot_benchmarks.jl --run --sweep full\n\n(Image: Bond dimension and construction time versus system size)\n\nAlong the chain the bond dimension rises over the first L_y sites — the length of one ring — and then sits on a flat plateau however long the cylinder is:\n\n(Image: Bond dimension profile along the chain)\n\n\n\nThis page was generated using Literate.jl.","title":"Scaling"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"","title":"Reference"},{"category":"type","location":"reference/#OpSum.BipartiteAlgorithm","page":"Reference","text":"BipartiteAlgorithm()\n\nAlgorithm selector: bipartite graph / minimum vertex cover (the default).\n\nLossless, and minimal among all MPOs with the same sparsity pattern. Selects the VertexCover bond-basis strategy.\n\n\n\n\n\n","title":"OpSum.BipartiteAlgorithm"},{"category":"type","location":"reference/#OpSum.BondStrategy","page":"Reference","text":"BondStrategy\n\nSupertype of the reduced-MPO bond-basis strategies: VertexCover, IndependentSVD and SequentialSVD. A strategy decides, at each bond, which compressed basis the sweep keeps; everything else about the sweep is shared.\n\n\n\n\n\n","title":"OpSum.BondStrategy"},{"category":"type","location":"reference/#OpSum.ITOGraph","page":"Reference","text":"ITOGraph{I}\n\nThe persistent bipartite graph over an ITOTermTable, handed from one site step to the next by _at_site!. A right vertex is a suffix class (identified by a representative term id); classes enter at their term's first active site and thereafter only merge. The current bipartite graph (for the bond i-1 → i about to be processed) is lefts ↔ right vertices, with per-left-vertex adjacency lists radj/wadj (right-vertex id, scalar weight); zero-weight entries are never kept.\n\nFields split into three groups:\n\nfixed suffix-class machinery: K (the term table's arity) and sufid, the interned id of every contiguous column suffix of tt — see _suffix_ids;\npersistent right-vertex state (shrinks via the suffix-merge): rrepr (representative term id) plus the monotone cursor rcur/rbond that turns \"the suffix path from site i+1\" into an O(1) two-word signature;\nthe current bipartite graph: lefts, radj, wadj, and nlinks (incoming bond dimension);\nlazy-insertion state (see _promote_pending!): lazy, firstsite, pend_at, pendbysig, inserted, nremaining, and the per-bond rsent (sentinel right-vertex id, 0 if none), startleft (the left vertex the sentinel hangs off, 0 if none) and startidx (the outgoing bond index of the start channel);\nfinish-channel bookkeeping, the mirror image of the start channel and the other half of what Jordan emission needs (jordanmpo.jl): rfinish (the right vertex whose suffix class is exhausted at the trivial charge, 0 if there is none at this bond), finishleft (the left vertex that class's pass-through enters on, 0 if none) and finishidx (the outgoing bond index of the finish channel);\nper-site scratch reused across the sweep so the site step allocates nothing per bond: slot and vlocal (right-vertex id → local index, for the remap and the per-component numbering), firstleft (right vertex → first incident left vertex, all the cover needs to read off a covered right vertex's bond charge without transposing the adjacency), remap, and siggroups.\n\n\n\n\n\n","title":"OpSum.ITOGraph"},{"category":"method","location":"reference/#OpSum.ITOGraph-Union{Tuple{I}, Tuple{OpSum.ITOTermTable{I}, Int64}} where I","page":"Reference","text":"ITOGraph(tt::ITOTermTable{I}, N; lazy = true) -> ITOGraph{I}\n\nSeed the persistent graph (mirrors ITensor's MPOGraph(os)): intern the column suffixes, create the right vertices, and bucket the initial left vertices by the site-1 key against the single left-boundary link. Θ(M·K), with no length-N path and no sort.\n\nWith lazy = true (the default) only terms active at site 1 get a right vertex; the rest are represented collectively by a sentinel right vertex on the identity/start channel and are inserted when they become reachable (_promote_pending! / the injection in _build_next_graph!). That is what makes the sweep cost Θ(Σ_terms span) instead of Θ(N·M). lazy = false seeds every term eagerly, which is the form the SequentialSVD strategy uses (its per-bond dense SVD dominates anyway).\n\njordan = true additionally forces the finish class into every cover (_force_finish!), which is what Jordan emission needs and what irrep_mpo deliberately does not do.\n\n\n\n\n\n","title":"OpSum.ITOGraph"},{"category":"type","location":"reference/#OpSum.ITOKey","page":"Reference","text":"ITOKey{I<:Sector}\n\nAlphabet symbol of the charge-augmented automaton: op (the ITO letter or pass-through), bond (the bond charge out of this site, i.e. the running fusion up to and including it), and vertex (the fusion-vertex multiplicity label; 1 for multiplicity-free fusion). Prefixes agreeing on op and bond and vertex share a trie node, so the trie is a direct sum of per-bond-sector FSMs.\n\n\n\n\n\n","title":"OpSum.ITOKey"},{"category":"type","location":"reference/#OpSum.ITOTermTable","page":"Reference","text":"ITOTermTable{I<:Sector}\n\nFlat, sparse-per-term storage of a canonical ITO operator on an N-vertex chain: each term's active (site, ITOKey) factors in K×M matrices (sites zero-padded, ascending) plus a parallel coeffs vector. This is the only thing the reduced-MPO sweeps read.\n\nNote there is no fusion tree here, and none is needed: an ITOKey carries the running bond charge and vertex label at its position, and for the left-nested (caterpillar) coupling this algebra supports those are the tree — bondcharges/vertexlabels read them off it, _tree_from_bonds puts it back together. irrepkey.jl has the argument, including why the total charge alone would not do.\n\n\n\n\n\n","title":"OpSum.ITOTermTable"},{"category":"method","location":"reference/#OpSum.ITOTermTable-Union{Tuple{OpSum.TermSum{I}}, Tuple{I}} where I","page":"Reference","text":"ITOTermTable(H::TermSum)\n\nMaterialise H in normal form (canonicalize!) as the flat table the MPO sweep consumes, on the N = length(lattice(H)) sites it is defined over.\n\n\n\n\n\n","title":"OpSum.ITOTermTable"},{"category":"type","location":"reference/#OpSum.IndependentSVD","page":"Reference","text":"IndependentSVD(trunc = nothing)\n\nBond-basis strategy: every bond is compressed independently, by an SVD of that bond's coefficient matrix over the raw prefix/suffix classes of the term table.\n\ntrunc is a MatrixAlgebraKit.TruncationStrategy (e.g. truncrank, trunctol) or nothing for the lossless default. Because the bonds do not see each other, truncrank(k) means k retained indices per bond, whatever the neighbouring bonds did. Contrast SequentialSVD.\n\n\n\n\n\n","title":"OpSum.IndependentSVD"},{"category":"type","location":"reference/#OpSum.LeftVertex","page":"Reference","text":"LeftVertex{I}\n\nA left (prefix) vertex of the persistent ITO graph: it entered the current site on incoming bond index link and applies the on-site ITO key key = (op, bond, vertex) here. The non-abelian analogue of ITensorMPOConstruction's LeftVertex(link, op_id, needs_JW); the fermion/JW-string slot is intentionally omitted, because in the symmetric setting the JW string is subsumed by the sector structure (the odd-parity charge on the virtual bond does that bookkeeping).\n\n\n\n\n\n","title":"OpSum.LeftVertex"},{"category":"type","location":"reference/#OpSum.OperatorBasis","page":"Reference","text":"abstract type OperatorBasis\n\nSupertype for on-site operator alphabets — the letters an SiteOperator is a combination of. The one concrete alphabet is IrrepOperator, the irreducible tensor operators of a TensorKit sector.\n\n\n\n\n\n","title":"OpSum.OperatorBasis"},{"category":"type","location":"reference/#OpSum.SVDBondAlgorithm","page":"Reference","text":"SVDBondAlgorithm(trunc = nothing; sweep = IndependentSVD)\n\nAlgorithm selector: SVD-based bond subspace selection.\n\ntrunc is a MatrixAlgebraKit.TruncationStrategy (e.g. truncrank, trunctol) or nothing for the lossless default. Each bond's coefficient matrix is assembled as a charge-graded TensorMap, so svd_trunc does the per-sector SVD and the truncation globally across sectors, respecting the quantum dimensions.\n\nsweep picks the truncation semantics, and only matters when trunc !== nothing:\n\nsweep meaning of truncrank(k)\nIndependentSVD (default) k retained indices per bond; every bond is compressed on the raw prefix/suffix classes, independently of its neighbours\nSequentialSVD k retained indices after upstream truncation; each bond is compressed in the basis left over from the previous one, so an aggressive early truncation can starve downstream bonds\n\nBoth are exact when trunc === nothing, and then produce the same per-sector bond dimensions. SVDBondAlgorithm(alg::BondStrategy) names the strategy (and its truncation) directly.\n\n\n\n\n\n","title":"OpSum.SVDBondAlgorithm"},{"category":"type","location":"reference/#OpSum.SequentialSVD","page":"Reference","text":"SequentialSVD(trunc = nothing)\n\nBond-basis strategy: a sequential left-to-right sweep (ITensor's QR-backend sweep), where each bond is compressed in the basis left over from the bond before it.\n\ntrunc is a MatrixAlgebraKit.TruncationStrategy or nothing for the lossless default. Losslessly this agrees with IndependentSVD — same per-sector bond dimensions, same operator — but under truncation the two differ by design: here truncrank(k) means \"k indices after whatever upstream truncation already threw away\", so an aggressive early truncation can starve the downstream bonds of the states they would need. In exchange the sweep is incremental: it never re-derives a bond's classes from scratch, and it reuses the persistent graph.\n\n\n\n\n\n","title":"OpSum.SequentialSVD"},{"category":"type","location":"reference/#OpSum.SiteOperator","page":"Reference","text":"SiteOperator{I<:Sector}\n\nAn operator on a single site, not yet placed on the lattice: a sparse combination of ITO alphabet letters, Σₖ coeffs[k] · letters[k].\n\nObtained from project, matrixunit, spin or scalarop, and combined with ordinary arithmetic (+ - * /). Place it on the lattice with A[i], which produces a TermSum.\n\nThe identity is represented as the passthrough sentinel letter, so scalarop(c, I) is c times that letter rather than a distinct kind of object.\n\n\n\n\n\n","title":"OpSum.SiteOperator"},{"category":"type","location":"reference/#OpSum.Term","page":"Reference","text":"Term{I<:Sector}\n\nA single term: sites (ascending, unique), one ITOKey each — the letter plus the running bond charge and vertex label at that position — and a reduced coeff.\n\narity(t) is 0 for the identity, 1 for a field, ≥ 2 for left-nested (caterpillar) coupling, whose tree is derived from the running bond charges rather than stored (tree, irrepkey.jl). Multiplicity-free fusion only; GenericFusion is deferred.\n\nNever mutated, so terms may share the vectors they were built from. ==/hash ignore the coefficient, which is what makes summing coincident terms well defined.\n\n\n\n\n\n","title":"OpSum.Term"},{"category":"type","location":"reference/#OpSum.TermSum","page":"Reference","text":"TermSum{I<:Sector}\n\nThe compressible ITO operator: a list of Terms plus the lattice they live on. The lattice is mandatory — instantiate and irrep_mpo_tensors need the space of every site, idle ones included, which no term can know — so a TermSum comes from opsum, not from placement, and irrep_mpo / instantiate / jordan_mpo_tensors need no sites.\n\nTerms are appended as given, so the list is a bag until canonicalize! normalises it in place. length(H), iteration and ≈ therefore report the canonical operator, never the append count (nterms_raw(H) is that, for tests).\n\n\n\n\n\n","title":"OpSum.TermSum"},{"category":"type","location":"reference/#OpSum.Terms","page":"Reference","text":"Terms{I<:Sector}\n\nA bag of Terms with no lattice: what A[i], couple, dot and project return, and what +, -, * and / combine. Nothing is deduplicated here — the normal form comes later, on the TermSum. opsum makes it compressible.\n\n\n\n\n\n","title":"OpSum.Terms"},{"category":"type","location":"reference/#OpSum.VertexCover","page":"Reference","text":"VertexCover()\n\nBond-basis strategy: a minimum vertex cover of the bond's bipartite (prefix, suffix) graph, per connected component. Lossless, and minimal among all MPOs with the same sparsity pattern. This is what BipartiteAlgorithm selects.\n\n\n\n\n\n","title":"OpSum.VertexCover"},{"category":"method","location":"reference/#Base.adjoint-Union{Tuple{OpSum.TermSum{I}}, Tuple{I}} where I","page":"Reference","text":"adjoint(H::TermSum) -> TermSum\nH'\n\nThe hermitian conjugate of H, so that H + H' is hermitian and (H')' ≈ H:\n\nF = fermion_ops()\nT = opsum(fill(V, N), (-t * couple(F.cd[i], F.c[i + 1]) for i in 1:(N - 1)))\nH = T + T'                                 # ≡ -t Σᵢ (c†ᵢcᵢ₊₁ + c†ᵢ₊₁cᵢ)\n\nDefined on a TermSum and not on a Terms bag because it needs the physical spaces: the adjoint of an alphabet letter is generally a combination of the dual charge's letters, which only the space knows. Bind the lattice with opsum first.\n\nEvery term must have total charge unit(I) — the case a Hamiltonian term is in. A charged term's adjoint lives in the dual charge sector, which is a different object than this signature can return.\n\nEach distinct term shape costs one projection, memoised, so conjugating a whole Hamiltonian is O(number of distinct shapes) rather than O(number of terms).\n\n\n\n\n\n","title":"Base.adjoint"},{"category":"method","location":"reference/#Base.append!-Union{Tuple{I}, Tuple{OpSum.TermSum{I}, Vararg{Any}}} where I","page":"Reference","text":"append!(H::TermSum, terms...) -> H\n\nAppend terms to H in place, in one pass — the linear way to accumulate into an existing operator. Same argument forms as opsum.\n\n\n\n\n\n","title":"Base.append!"},{"category":"method","location":"reference/#Base.isapprox-Union{Tuple{I}, Tuple{OpSum.TermSum{I}, OpSum.TermSum{I}}} where I","page":"Reference","text":"isapprox(a::TermSum, b::TermSum; kwargs...)\na ≈ b\n\nWhether two operators carry the same terms with matching coefficients: the canonical term sets must be equal (a dropped term is never \"approximately\" absent) and the coefficients ≈. Lattices are not compared, so an operator reconstructed by mpo_terms — which knows the bonds but not the spaces — compares equal to the one it came from. This is the faithfulness check.\n\n\n\n\n\n","title":"Base.isapprox"},{"category":"method","location":"reference/#LinearAlgebra.dot-Union{Tuple{I}, Tuple{OpSum.Terms{I}, OpSum.Terms{I}}} where I","page":"Reference","text":"dot(a::Terms, b::Terms)\na · b\n\nThe Cartesian two-body scalar product of two single-site ITO operators: singlet coupling couple(a, b; to = unit(I)) times the Cartesian factor -√dim(c) (the identity Sᵢ·Sⱼ = -√3 [S⊗S]⁽⁰⁾ with -√3 = -√dim(spin-1)).\n\nEither site order is accepted, for any symmetry: two legs coupling to the unit sector is the one case where reordering needs no F-move, whatever the fusion style, so dot is defined where the general out-of-order couple is not. The Cartesian factor is read off the operator that ends up on the left, which changes nothing — the two charges must fuse to the unit sector, i.e. be each other's dual, and dual sectors have equal quantum dimension. The braiding phase does not always cancel, though: dot(a, b) == R · dot(b, a) with R = Rsymbol(cₐ, c_b, unit), which is +1 for bosonic integer charges (so spin, and hence every spin chain here, is order-independent) and -1 for a pair of odd fermionic charges or of half-integer SU(2) charges.\n\nUnlike couple this does not distribute over composite operands: the Cartesian factor -√dim(c) is per-letter, so it has no meaning for an operator mixing several charges. Use couple for those.\n\n\n\n\n\n","title":"LinearAlgebra.dot"},{"category":"method","location":"reference/#OpSum._force_finish!-Tuple{Any, Any, Vector{Vector{Int64}}, Int64}","page":"Reference","text":"_force_finish!(cUbits, cVbits, localadj, pf) -> Bool\n\nMove the finish class (component-local right index pf) into the cover, and drop every left vertex that this makes redundant. Returns whether anything changed.\n\nWhy it is needed. A minimum cover is free to cover the finish class from the left instead, and does so exactly when it can: if the finish class and one incident left vertex form an isolated matched pair — what a bond at which nothing new finishes looks like — König's alternating search visits neither, so the left vertex is the one that lands in the cover. The resulting bond index still means \"already finished\", but it is the covered-left index of a term's last factor, so it emits that factor's letter weighted by the term coefficient, not the identity. Jordan form needs a channel that emits exactly 1 · id from the previous bond's finish index, and that is only the covered-right reading. Since a covered-left finish then has to be padded around, forcing is never a loss: it grows the cover by at most one (a cover cannot shrink below the minimum, so at most one left vertex is dropped), and it saves exactly the one padded index it would otherwise have cost. Dropping a left vertex whose neighbours are all covered is safe — a left vertex is only needed for the edges its neighbours do not cover.\n\nThe start channel needs no counterpart: §2.2 of research/persistent-graph-mpo.md shows König never covers a degree-one right vertex, so the sentinel's left vertex L₀ is always covered-left already. L₀ is also never adjacent to the finish class — every class it carries has at least one factor left to place — so forcing cannot remove it.\n\n\n\n\n\n","title":"OpSum._force_finish!"},{"category":"method","location":"reference/#OpSum._irrep_channels-Tuple{OpSum.ITOTermTable, Int64, Union{OpSum.VertexCover, OpSum.SequentialSVD}}","page":"Reference","text":"_irrep_channels(tt::ITOTermTable, N, strategy::BondStrategy) -> (Ws, bondsectors, starts, finishes)\n\n_irrep_sweep with the identity-channel indices _irrep_graph_channels documents, and with the finish class forced into the cover. The independent-SVD pass has no persistent bond identity at all, so it reports none and Jordan emission pads every channel.\n\n\n\n\n\n","title":"OpSum._irrep_channels"},{"category":"method","location":"reference/#OpSum._irrep_graph_channels-Union{Tuple{I}, Tuple{OpSum.ITOTermTable{I}, Int64, Union{OpSum.VertexCover, OpSum.SequentialSVD}, Bool}} where I","page":"Reference","text":"_irrep_graph_channels(tt::ITOTermTable{I}, N, strategy, jordan) -> (Ws, bondsectors, starts, finishes)\n\n_irrep_graph_sweep plus the per-bond identity-channel indices that Jordan emission (jordanmpo.jl) needs: starts[i] / finishes[i] are the bond indices of the start (\"nothing placed yet\") and finish (\"everything placed\") channels at the bond to the right of site i, or 0 where the cover did not spend an index on that channel. Only VertexCover has them — an SVD bond basis is a mixture of prefix states, in which neither channel is a basis vector — so SequentialSVD reports 0 throughout and every channel gets padded.\n\njordan = true also forces the finish class into every cover (_force_finish!), which is why this is a separate entry point rather than extra return values on _irrep_graph_sweep: it can change the bond basis, and irrep_mpo promises the unconstrained minimum.\n\n\n\n\n\n","title":"OpSum._irrep_graph_channels"},{"category":"method","location":"reference/#OpSum._irrep_graph_sweep-Tuple{OpSum.ITOTermTable, Int64, Union{OpSum.VertexCover, OpSum.SequentialSVD}}","page":"Reference","text":"_irrep_graph_sweep(tt::ITOTermTable{I}, N, strategy) -> (Ws, bondsectors)\n\nThe persistent-graph reduced-MPO sweep, run with a bond-basis strategy (VertexCover or SequentialSVD). Produces the (Ws::Vector{SparseMatrixCSC{SiteOperator{I}, Int}}, bondsectors::Vector{Vector{I}}) contract that mpo_terms / irrep_mpo_tensors consume.\n\n\n\n\n\n","title":"OpSum._irrep_graph_sweep"},{"category":"method","location":"reference/#OpSum._irrep_independent_svd-Union{Tuple{I}, Tuple{OpSum.ITOTermTable{I}, Int64, Any}} where I","page":"Reference","text":"_irrep_independent_svd(tt::ITOTermTable{I}, N, trunc) -> (Ws, bondsectors)\n\nPer-bond-independent SVD reduced-MPO sweep: one coefficient matrix per bond over the raw prefix/suffix classes, keep the left singular vectors as that bond's compressed basis, then project the vertex operators into the compressed bases (W_op = U_{i-1}' · C_op · U_i per ITO letter).\n\nThe ITO-specific part: each bond's coefficient matrix is a charge-graded TensorMap C_b : Ppre ← Psuf, both spaces graded by the running bond charge, so C_b is block-diagonal in that charge and svd_trunc does the per-sector SVD and the global-across-sectors truncation at once (respecting the quantum dimensions); the retained bond space gives bondsectors directly. The compression acts on the symbolic bond coefficients only — entries stay ITO letters times scalars — so the output (Ws, bondsectors) feeds irrep_mpo_tensors unchanged.\n\ntrunc === nothing ⇒ lossless default, and then this agrees with SequentialSVD on the internal bonds. Under truncation they differ by design; see SVDBondAlgorithm.\n\nClasses are named, not materialised: the prefix class at bond b is the interned prefix factor list (_prefix_ids — the pass-through fill of every idle site to its left is fixed by that list), and the suffix class is the two-word signature (sufid, running charge) the graph sweep uses. Only the current bond's Θ(M) class assignment is held at a time, plus one interned id → dense column dictionary per bond (Θ(Σ_b n_pre(b)), i.e. Θ(N) for a finite-range model) so that phase 3 can address the same columns phase 2 built.\n\n\n\n\n\n","title":"OpSum._irrep_independent_svd"},{"category":"method","location":"reference/#OpSum._irrep_sweep-Tuple{OpSum.ITOTermTable, Int64, Union{OpSum.VertexCover, OpSum.SequentialSVD}}","page":"Reference","text":"_irrep_sweep(tt::ITOTermTable{I}, N, strategy::BondStrategy) -> (Ws, bondsectors)\n\nRun the reduced-MPO compression of tt over N sites with the given bond-basis strategy. This is the single entry point irrep_mpo (irrepmpo.jl) dispatches to; the strategy decides whether that is the persistent-graph sweep or the independent per-bond pass.\n\n\n\n\n\n","title":"OpSum._irrep_sweep"},{"category":"method","location":"reference/#OpSum._prefix_ids-Union{Tuple{OpSum.ITOTermTable{I}}, Tuple{I}} where I","page":"Reference","text":"_prefix_ids(tt::ITOTermTable{I}) -> Matrix{Int}\n\nIntern every term's contiguous column prefixes — the mirror image of _suffix_ids. preid[j, t] is a dense integer id for the factor list 1:j-1 of term t (so preid[1, t] == 0, the empty prefix), built top-down in Θ(M·K).\n\nEquality of ids is equality of the prefix path o_t[1:b], with no charge component needed: at every idle site of the prefix _op_at_ito fills in a pass-through whose running charge is fixed by the factors to its left, i.e. by the factor list itself. (The suffix is the asymmetric one — the idle sites before its first remaining factor carry the charge accumulated by the prefix, which is why _signature! pairs sufid with the running charge.)\n\nUsed by the IndependentSVD sweep, which classifies both sides of every bond from scratch.\n\n\n\n\n\n","title":"OpSum._prefix_ids"},{"category":"method","location":"reference/#OpSum._promote_pending!-Union{Tuple{I}, Tuple{OpSum.ITOGraph{I}, Int64}} where I","page":"Reference","text":"_promote_pending!(g, i)\n\nInsert every not-yet-started term whose suffix class coincides with a live one at bond i.\n\nThis is the subtle half of lazy insertion. _op_at_ito fills idle sites with a pass-through carrying the running bond charge, so a started term whose accumulated charge has fused back to unit(I) is indistinguishable, over its idle sites, from a term that has not started yet. If its remaining factors then coincide with the whole content of a pending term, the two suffix classes are genuinely equal and the eager sweep merges them — covering the shared right vertex instead of spending a bond index. That merge has to be reproduced, and at the earliest bond where it applies; suffix-equality-from-i+1 is monotone in i, so probing every bond finds it exactly once.\n\nCost: one hash probe per live right vertex (pendbysig is keyed injectively by a term's pre-start signature, so a hit names a single term).\n\n\n\n\n\n","title":"OpSum._promote_pending!"},{"category":"method","location":"reference/#OpSum._suffix_ids-Union{Tuple{OpSum.ITOTermTable{I}}, Tuple{I}} where I","page":"Reference","text":"_suffix_ids(tt::ITOTermTable{I}) -> Matrix{Int}\n\nIntern every term's contiguous column suffixes. tt.sites columns are ascending and zero-padded, so a term's active factors at sites > i are always a suffix j₀:K of its column; sufid[j, t] is a dense integer id for the factor list j:K of term t, with 0 for the exhausted suffix. Built bottom-up in Θ(M·K) — the replacement for materialising a length-N path per term.\n\nEquality of ids is equality of the remaining factor list. That is not by itself equality of the suffix path: _op_at_ito fills idle sites with a pass-through carrying the running bond charge, and the idle sites before the first remaining factor carry the charge accumulated so far. So a suffix path is identified by the pair (sufid[j₀, t], running bond charge) — see _signature.\n\n\n\n\n\n","title":"OpSum._suffix_ids"},{"category":"method","location":"reference/#OpSum.arity-Tuple{OpSum.Term}","page":"Reference","text":"arity(t::Term) -> Int\n\nThe number of active (charged) sites of t.\n\n\n\n\n\n","title":"OpSum.arity"},{"category":"method","location":"reference/#OpSum.bipartite_connected_components-Tuple{Vector{Vector{Int64}}, Int64}","page":"Reference","text":"bipartite_connected_components(adjU::Vector{Vector{Int}}, nV::Int)\n    -> (us_of_component::Vector{Vector{Int}}, vs_of_component::Vector{Vector{Int}})\n\nConnected components of the bipartite graph with left vertices 1:length(adjU), right vertices 1:nV, and edges u => v for v in adjU[u]. Each component's us/vs are the global left/right vertex ids belonging to it, both in ascending order. A right vertex with no incident edge is dropped (it belongs to no component); every left vertex is assumed to have at least one edge.\n\n\n\n\n\n","title":"OpSum.bipartite_connected_components"},{"category":"method","location":"reference/#OpSum.bondcharges-Union{Tuple{TensorKit.FusionTree{I, K}}, Tuple{K}, Tuple{I}} where {I, K}","page":"Reference","text":"bondcharges(f::FusionTree{I,K})\n\nRunning bond charges b₁…b_K of the caterpillar tree (b₁ = uncoupled[1], bₖ = innerlines[k-1] for 2 ≤ k ≤ K-1, b_K = coupled); element k is the bond charge out of active position k.\n\n\n\n\n\n","title":"OpSum.bondcharges"},{"category":"method","location":"reference/#OpSum.canonicalize!-Tuple{OpSum.TermSum}","page":"Reference","text":"canonicalize!(H::TermSum) -> H\n\nPut H in normal form in place: sort its terms (by sites, then keys, so coincident ones become adjacent), sum coincident ones, drop cancelled ones.\n\nAssumes nothing about its input, so no flag can fall out of step with the terms. It is idempotent, but a repeat call re-sorts rather than returning early — hence the sweep normalises once, at the ITOTermTable boundary.\n\n\n\n\n\n","title":"OpSum.canonicalize!"},{"category":"method","location":"reference/#OpSum.canonicalize!-Tuple{OpSum.Terms}","page":"Reference","text":"canonicalize!(ts::Terms) -> ts\n\nThe same normal form on a bag of terms. Bags are not normalised as they are built, so this is what ≈ and == on them go through.\n\n\n\n\n\n","title":"OpSum.canonicalize!"},{"category":"method","location":"reference/#OpSum.caterpillar_trees-Union{Tuple{I}, Tuple{K}, Tuple{NTuple{K, I}, I}} where {K, I<:TensorKitSectors.Sector}","page":"Reference","text":"caterpillar_trees(charges::NTuple{K,I}, total::I) -> Vector{FusionTree{I,K}}\n\nAll left-nested caterpillar fusion trees of the per-site charges fusing to total, in TensorKit's canonical order (finite, bounded — delegated to TensorKit fusiontrees).\n\n\n\n\n\n","title":"OpSum.caterpillar_trees"},{"category":"method","location":"reference/#OpSum.couple-Union{Tuple{I}, Tuple{OpSum.Terms{I}, OpSum.Terms{I}}} where I","page":"Reference","text":"couple(a::Terms, b::Terms; to = unit(I))\ncouple(a::Terms, b::Terms, cs::Terms...; to = unit(I))   # abelian only\n\nLeft-nested (caterpillar) irrep coupling: extend the composite a (any K ≥ 1 sites, with its running coupling charges) by one single-site operator b, fusing the running total of a with b's charge to to at a new vertex. to defaults to the unit sector, which is what a term of a Hamiltonian needs — pass it explicitly to build a charged object.\n\nUnder an abelian symmetry the operands may be written in any site order: the stored form is site-ordered, so an out-of-order leg is inserted rather than appended, and the braiding phase that costs is inserted with it. For a fermionic sector that phase is the anticommutation sign, so\n\ncouple(cd[i + 1], c[i]) == -couple(c[i], cd[i + 1])       # both spellings are available\n\nand the hand-written sign that used to be mandatory is now only a way to get it wrong. Under a non-abelian symmetry reordering needs F-moves and still throws: each operand after the first must then act to the right of every site before it. (dot accepts either order for any symmetry — with only two legs coupling to the unit sector, no F-move arises.)\n\nBoth operands may be composite (several terms, e.g. from project): the coupling distributes over every pair of terms, and pairs whose charges cannot fuse to to are dropped. It is an error if no pair fuses. Each term of a must carry at least one charged operator, and each term of b must be a single charged site.\n\nFor K  3 the intermediate channels matter. Under an abelian symmetry (UniqueFusion: U₁, ℤₙ, FermionNumber, Trivial, products thereof) every intermediate is forced by the charges, so the variadic form does the whole chain for you:\n\ncouple(cd[1], c[2], cd[3], c[4])        # a charge-neutral four-fermion term\n\nUnder a non-abelian symmetry the intermediates are genuine freedom and the variadic form throws: nest instead, naming each channel, so the choice is explicit and readable back off the term:\n\ncouple(couple(S[1], S[2]; to = SU2Irrep(1)), S[3]; to = SU2Irrep(0))\n\nThis is the bare fusion coupler — it carries no normalization factor (reduced coeff = va·vb). The Cartesian scalar-product convention lives in dot, not here. Multi-channel (GenericFusion) coupling and tree-structured (via) coupling are deferred.\n\n\n\n\n\n","title":"OpSum.couple"},{"category":"method","location":"reference/#OpSum.fermion_ops-Union{Tuple{I}, Tuple{TensorKit.ElementarySpace, I, I}} where I<:TensorKitSectors.Sector","page":"Reference","text":"fermion_ops(V = Vect[FermionNumber](0 => 1, 1 => 1)) -> NamedTuple\nfermion_ops(V::ElementarySpace, vac, occ)\n\n(; c, cd, n) of a spinless fermionic mode: annihilation c, creation c† and number n̂, as matrix units between the empty sector vac and the occupied sector occ of V.\n\nNo Jordan–Wigner string is involved anywhere: the odd-parity charge flowing along the virtual bond carries the anticommutation, which is why these are ordinary single-letter operators. Both site orders of a hopping term are available — couple inserts the sign — so\n\nF = fermion_ops()\nH = -t * sum(couple(F.cd[i], F.c[i + 1]) + couple(F.cd[i + 1], F.c[i]) for i in 1:(N - 1))\n\nThe one-argument form infers vac/occ for Vect[FermionNumber] only; any other graded space has to name them.\n\nSee also spin_ops, matrixunit.\n\n\n\n\n\n","title":"OpSum.fermion_ops"},{"category":"method","location":"reference/#OpSum.hopcroft_karp-Tuple{Vector{Vector{Int64}}, Int64, Int64}","page":"Reference","text":"hopcroft_karp(adjU, nU, nV)\n\nCompute a maximum matching in a bipartite graph using the Hopcroft–Karp algorithm, in O(E√V).\n\nArguments\n\nadjU::Vector{Vector{Int}}:   Adjacency list for the left part U.   adjU[u] is a list of neighbors v in the right part V (1-based indices).\nnU::Int: number of vertices on the left side (U = 1:nU)\nnV::Int: number of vertices on the right side (V = 1:nV)\n\nReturns\n\npairU::Vector{Int}: size nU, pairU[u] is the matched v in V or 0 if free.\npairV::Vector{Int}: size nV, pairV[v] is the matched u in U or 0 if free.\nmatching_size::Int: size of the maximum matching.\n\n\n\n\n\n","title":"OpSum.hopcroft_karp"},{"category":"function","location":"reference/#OpSum.instantiate","page":"Reference","text":"instantiate(op, V)\n\nMaterialize a symbolic operator into its concrete form on the physical space V (a TensorKit TensorMap). Implemented per alphabet; see instantiate(::IrrepOperator, ::ElementarySpace).\n\n\n\n\n\n","title":"OpSum.instantiate"},{"category":"method","location":"reference/#OpSum.instantiate-Tuple{OpSum.TermSum}","page":"Reference","text":"instantiate(H::TermSum)\ninstantiate(ts::Terms, sites::AbstractVector{<:ElementarySpace})\n\nMaterialize the operator into a TensorKit TensorMap over its lattice (the dense oracle), summing each term. Supports identity (K=0), single-site field (K=1), and left-nested (caterpillar) coupling of any K ≥ 2 sites.\n\n\n\n\n\n","title":"OpSum.instantiate"},{"category":"method","location":"reference/#OpSum.instantiate-Union{Tuple{I}, Tuple{OpSum.SiteOperator{I}, TensorKit.ElementarySpace}} where I","page":"Reference","text":"instantiate(O::SiteOperator, V::ElementarySpace)\n\nMaterialize a single-site operator over the ITO alphabet as the coefficient-weighted sum of its letters' tensors. The pass-through letter instantiates to id(V), so a scalar c·𝟙 comes out as c * id(V) structurally.\n\n\n\n\n\n","title":"OpSum.instantiate"},{"category":"function","location":"reference/#OpSum.irrep_mpo","page":"Reference","text":"irrep_mpo(H::TermSum[, alg]) -> (Ws, bondsectors)\n\nCompress the ITO Hamiltonian H over the N = length(lattice(H)) sites it is defined on into a reduced MPO. The lattice travels with H (see opsum), which is also where every letter was checked against the space of the site it acts on.\n\nReturns Ws::Vector{SparseMatrixCSC{SiteOperator{I}, Int}} (one reduced bond matrix per site; entries are ITO letters times reduced coefficients) and bondsectors::Vector{ Vector{I}} where bondsectors[i] gives the bond charge of each bond index to the right of site i (so size(Ws[i], 2) == length(bondsectors[i])). The left boundary (bond 0) is a single trivial-charge index.\n\nalg is the algorithm selector: BipartiteAlgorithm() (the default, lossless minimum vertex cover) or SVDBondAlgorithm(trunc; sweep), whose sweep picks between the two truncation semantics (IndependentSVD, the default and the historical behaviour, versus SequentialSVD). Each selector names a BondStrategy; the sweeps live in irrepgraph.jl.\n\n\n\n\n\n","title":"OpSum.irrep_mpo"},{"category":"method","location":"reference/#OpSum.irrep_mpo_tensors-Union{Tuple{I}, Tuple{Vector{<:SparseArrays.SparseMatrixCSC{OpSum.SiteOperator{I}}}, Array{Vector{I}, 1}, Any}} where I","page":"Reference","text":"irrep_mpo_tensors(Ws, bondsectors, sites) -> Vector{<:AbstractTensorMap}\n\nAssemble the symmetric MPO from the reduced bond matrices + bond sectors (from irrep_mpo). Site tensor W_i : B_{i-1} ⊗ V_i ← V_i ⊗ B_i (MPSKit convention); the boundary bonds B_0, B_N are one-dimensional. Each reduced entry (l, r, letter, coeff) places the ITO letter's tensor into the (l → r) bond transition weighted by coeff, coupling the operator charge into the bond via the (forward) fusion b_L ⊗ c → b_R.\n\n\n\n\n\n","title":"OpSum.irrep_mpo_tensors"},{"category":"method","location":"reference/#OpSum.islossless-Tuple{OpSum.TermSum, Vararg{Any}}","page":"Reference","text":"islossless(H::TermSum[, alg]) -> Bool\n\nWhether the compressed MPO reproduces H exactly: reconstruct the operator the reduced MPO generates with mpo_terms and compare it against H with ≈, i.e. term set exactly and coefficients approximately.\n\nThis is the primary correctness check for a construction — cheap, purely symbolic, independent of N, and valid for fermionic sectors, where densifying is not a well-defined operation.\n\nOnly meaningful for lossless compression: after a truncating SVDBondAlgorithm false is the expected answer rather than a bug.\n\n\n\n\n\n","title":"OpSum.islossless"},{"category":"method","location":"reference/#OpSum.ispassthrough-Tuple{OpSum.IrrepTensorOperators.IrrepOperator}","page":"Reference","text":"ispassthrough(op::IrrepOperator)\n\nWhether op is the pass-through identity sentinel (index n = 0).\n\n\n\n\n\n","title":"OpSum.ispassthrough"},{"category":"function","location":"reference/#OpSum.jordan_mpo_tensors","page":"Reference","text":"jordan_mpo_tensors(H::TermSum[, alg]) -> Vector{<:SparseBlockTensorMap}\n\nCompress H into a reduced MPO (as irrep_mpo) and emit it in Jordan form: one BlockTensorKit.SparseBlockTensorMap per site, W_i : B_{i-1} ⊗ V_i ← V_i ⊗ B_i, whose virtual legs are SumSpaces carrying one level Vect[I](charge => 1) per bond index, ordered as\n\n(start channel, everything else, finish channel)\n\nwith the identity at (1, 1) and (end, end). The boundary bonds B_0 and B_N are one-dimensional and trivially charged, so H must have total charge unit(I).\n\nDiagonal pass-through entries with unit coefficient are emitted as TensorKit.BraidingTensors, which is what lets a consumer store them as scalars rather than dense blocks.\n\nThis is the shape MPSKit's JordanMPOTensor / FiniteMPOHamiltonian consume; OpSum does not depend on MPSKit, and nothing here does more than name the convention. Use irrep_mpo_tensors for the dense per-site TensorMaps instead.\n\nalg defaults to BipartiteAlgorithm. An SVDBondAlgorithm bond basis is a mixture of prefix states in which neither identity channel is a basis vector, so both are padded at every bond; a truncation aggressive enough to empty a bond is rejected, since a Jordan-form MPO with a zero-dimensional bond cannot carry its identity corners.\n\n\n\n\n\n","title":"OpSum.jordan_mpo_tensors"},{"category":"method","location":"reference/#OpSum.jordan_mpo_tensors-Union{Tuple{I}, Tuple{Vector{<:SparseArrays.SparseMatrixCSC{OpSum.SiteOperator{I}}}, Array{Vector{I}, 1}, Vector{Int64}, Vector{Int64}, Any}} where I","page":"Reference","text":"jordan_mpo_tensors(Ws, bondsectors, starts, finishes, sites) -> Vector{<:SparseBlockTensorMap}\n\nThe reduced-data form of jordan_mpo_tensors: (Ws, bondsectors) as irrep_mpo returns them, plus the per-bond start/finish channel indices from _irrep_channels (0 where the cover did not spend an index — that channel is then padded).\n\nTwo structural invariants are checked per site rather than assumed, because a violation would be silent: nothing may enter the start column except from the start row, and the finish row may emit into nothing but the finish column. Both follow from how the two channels are read off the cover, but they are the properties a consumer's block accessors rely on, and a wrong one drops operators on the floor instead of erroring.\n\n\n\n\n\n","title":"OpSum.jordan_mpo_tensors"},{"category":"method","location":"reference/#OpSum.lattice-Tuple{OpSum.TermSum}","page":"Reference","text":"lattice(H::TermSum) -> Vector{<:ElementarySpace}\n\nThe physical spaces H is defined on, one per site. Always present.\n\n\n\n\n\n","title":"OpSum.lattice"},{"category":"method","location":"reference/#OpSum.matrixunit-Union{Tuple{I}, Tuple{TensorKit.ElementarySpace, I, I}} where I<:TensorKitSectors.Sector","page":"Reference","text":"matrixunit(V::ElementarySpace, out::I, in::I) -> SiteOperator\n\nThe matrix unit outin on a space whose sectors are one-dimensional and appear with multiplicity one (abelian or fermionic: Vect[U₁], Vect[FermionNumber], and products thereof). The operator charge follows from the fusion out ⊗ in' → c, and the alphabet letters are derived by project rather than named by index.\n\nV = Vect[FermionNumber](0 => 1, 1 => 1)\nvac, occ = FermionNumber(0), FermionNumber(1)\nc  = matrixunit(V, vac, occ)   # annihilation\ncd = matrixunit(V, occ, vac)   # creation\nn  = matrixunit(V, occ, occ)   # number\n\nMemoised per (V, out, in), so the projection is paid once even when this is called inside a term loop. fermion_ops and spin_ops bundle the sets above.\n\n\n\n\n\n","title":"OpSum.matrixunit"},{"category":"method","location":"reference/#OpSum.min_vertex_cover_bipartite-Tuple{Vector{Vector{Int64}}, Int64, Int64}","page":"Reference","text":"min_vertex_cover_bipartite(adjU::Vector{Vector{Int}}, nU, nV)\nmin_vertex_cover_bipartite(A::AbstractMatrix{<:Real})\n\nMinimum vertex cover of a bipartite graph, via a Hopcroft–Karp maximum matching plus König's construction. Returns (coverU, coverV, pairU, pairV, matching_size) where coverU/coverV are boolean masks over the left/right vertices.\n\nThe adjacency-list method is the primary one and runs in O(E√V); the matrix method builds the adjacency lists (treating any nonzero as an edge) and forwards. Left vertices with no incident edge are never covered.\n\n\n\n\n\n","title":"OpSum.min_vertex_cover_bipartite"},{"category":"method","location":"reference/#OpSum.mpo_tensormap-Tuple{AbstractVector}","page":"Reference","text":"mpo_tensormap(Ts::AbstractVector) -> AbstractTensorMap\n\nContract a chain of MPO site tensors — as irrep_mpo_tensors returns them, or jordan_mpo_tensors after map(TensorMap, ·) — into the single N-site operator, in instantiate's leg convention: codomain o₁…o_N, domain i₁…i_N and the trailing total-charge leg. The trivial left boundary bond is dropped.\n\nThe tensor-level oracle, mpo_tensormap(irrep_mpo_tensors(irrep_mpo(H)..., lattice(H))) ≈ instantiate(H). Exponential in N, so small systems only — but it stays inside TensorKit and never materialises a dense array, so unlike convert(Array, ·) it is valid for fermionic sectors.\n\n\n\n\n\n","title":"OpSum.mpo_tensormap"},{"category":"method","location":"reference/#OpSum.mpo_terms-Union{Tuple{I}, Tuple{Vector{<:SparseArrays.SparseMatrixCSC{OpSum.SiteOperator{I}}}, Array{Vector{I}, 1}, Any}} where I","page":"Reference","text":"mpo_terms(Ws, bondsectors, sites) -> TermSum\n\nReconstruct the operator generated by a reduced MPO (inverse of irrep_mpo at the reduced level): enumerate every path through the bond matrices, multiply the reduced coefficients along it, and read the active ITO letters (skipping pass-through) together with their outgoing bond charges — which are exactly the caterpillar running bonds a term carries. sites is the lattice to hand back, since the bond data names charges but not physical spaces. For the lossless bipartite compression this recovers the original operator exactly: mpo_terms(irrep_mpo(H)..., lattice(H)) ≈ H is the faithfulness check, which islossless packages up.\n\n\n\n\n\n","title":"OpSum.mpo_terms"},{"category":"method","location":"reference/#OpSum.nterms_raw-Tuple{OpSum.TermSum}","page":"Reference","text":"nterms_raw(H::TermSum) -> Int\n\nThe number of appended terms, before coincident ones are summed. For tests; length(H) is the number of terms the operator actually has.\n\n\n\n\n\n","title":"OpSum.nterms_raw"},{"category":"method","location":"reference/#OpSum.ops-Union{Tuple{OpSum.Term{I}}, Tuple{I}} where I","page":"Reference","text":"ops(t::Term) -> Vector{IrrepOperator}\n\nThe alphabet letters of t, one per active site, in site order.\n\n\n\n\n\n","title":"OpSum.ops"},{"category":"method","location":"reference/#OpSum.opsum-Tuple{Any, Vararg{Any}}","page":"Reference","text":"opsum(sites, terms...) -> TermSum\n\nBind terms to the lattice sites (one physical space per site), in one pass over each argument, so building an M-term Hamiltonian costs Θ(M):\n\nH = opsum(fill(V, N), (J * dot(S[i], S[i + 1]) for i in 1:(N - 1)))\n\nEach argument may be a Term, a Terms bag, another TermSum on the same lattice, or any iterable of those, nested arbitrarily. Every letter is checked against the space of the site it acts on and every site against 1:length(sites) — the only place operators and spaces are confronted.\n\nopsum(sites) is the empty operator; add to it with + or append!.\n\n\n\n\n\n","title":"OpSum.opsum"},{"category":"method","location":"reference/#OpSum.passthrough-Union{Tuple{Type{I}}, Tuple{I}} where I<:TensorKitSectors.Sector","page":"Reference","text":"passthrough(::Type{I}) where {I<:Sector}\n\nThe distinguished structural pass-through identity symbol: trivial charge unit(I) with the sentinel index n = 0. Fills idle sites, injecting trivial charge and acting as id(V). Not one of the enumerated (c, n) alphabet letters (n ≥ 1) and not a type-level one.\n\n\n\n\n\n","title":"OpSum.passthrough"},{"category":"method","location":"reference/#OpSum.project-Tuple{TensorKit.AbstractTensorMap, Any}","page":"Reference","text":"project(h::AbstractTensorMap, sites; atol = 0, rtol = 100eps) -> Terms\n\nProject a symmetric K-site operator onto the ITO term basis — the inverse of instantiate. h must have one of the two shapes instantiate produces, with K = length(sites) = numout(h):\n\nh :  V_1 ⊗ … ⊗ V_K  ←  V_1 ⊗ … ⊗ V_K                          # charge-neutral\nh :  V_1 ⊗ … ⊗ V_K  ←  V_1 ⊗ … ⊗ V_K ⊗ Vect[I](tot => 1)      # total charge `tot`\n\nThe physical spaces are read off h; sites only supplies the labels, and must be strictly increasing (couple's left-to-right convention, which the downstream sweep relies on). The coefficients are exact inner products against an orthogonal, complete basis, so no fit is involved.\n\nCoefficients whose norm contribution falls at or below max(atol, rtol * norm(h)) are dropped; the result is then re-materialized and compared against h, and an ArgumentError is thrown if the residual exceeds that same tolerance. A projected Terms therefore provably represents its input. An operator that is zero (or entirely below tolerance) gives an empty bag. Bind it to a lattice with opsum to compress it.\n\nEvery returned term is active on all K sites: an on-site identity factor comes back as a trivial-charge letter, not as a shorter term. So project ∘ instantiate is the identity only for operators whose terms all have full support on sites.\n\njulia> using TensorKit\n\njulia> V = SU2Space(1//2 => 1);\n\njulia> h = OpSum.instantiate(couple(spin(V)[1], spin(V)[2]; to = SU2Irrep(0)), [V, V]);\n\njulia> length(project(h, [1, 2]))\n1\n\nSee also matrixunit, couple.\n\n\n\n\n\n","title":"OpSum.project"},{"category":"method","location":"reference/#OpSum.project-Tuple{TensorKit.AbstractTensorMap, TensorKit.ElementarySpace}","page":"Reference","text":"project(O::AbstractTensorMap, V::ElementarySpace; atol = 0, rtol = 100eps) -> SiteOperator\n\nExpand a single-site operator in the ITO alphabet of V, as an SiteOperator that is not yet placed on the lattice. O must be either V ← V or V ← V ⊗ Vect[I](c => 1). The result carries one letter per surviving alphabet element.\n\nBecause a SiteOperator supports ordinary arithmetic, composite on-site operators read the way you would write them on paper, and A[i] then places the whole expansion:\n\nSz = (matrixunit(V, up, up) - matrixunit(V, dn, dn)) / 2\nH  = sum(couple(Sz[i], Sz[i + 1]; to = unit(I)) for i in 1:(N - 1))\n\nNote that the identity is not an alphabet letter: project(id(V), V) returns a sum of trivial-charge letters (n ≥ 1) rather than scalarop(1, V) (which is the n = 0 pass-through sentinel). An operator that is zero (or entirely below tolerance) gives the empty SiteOperator, which does not retain the charge of the input.\n\n\n\n\n\n","title":"OpSum.project"},{"category":"method","location":"reference/#OpSum.scalarop-Union{Tuple{I}, Tuple{Number, Type{I}}} where I","page":"Reference","text":"scalarop(c::Number, V::ElementarySpace)\nscalarop(c::Number, ::Type{I<:Sector})\n\nA scalar (identity) local operator c·𝟙 over the ITO alphabet; instantiates structurally to c * id(V) (the identity is not an alphabet letter).\n\n\n\n\n\n","title":"OpSum.scalarop"},{"category":"method","location":"reference/#OpSum.sparse_from_dict-Union{Tuple{T}, Tuple{Dictionaries.AbstractDictionary{CartesianIndex{2}, T}, Tuple{Int64, Int64}}} where T","page":"Reference","text":"sparse_from_dict(dict::AbstractDictionary{CartesianIndex{2}}, sz) -> SparseMatrixCSC\n\nMaterialise a dict-of-keys accumulator (CartesianIndex(row, col) => value) as a sparse matrix of size sz. Keys are unique, so no duplicate combination happens; stored entries are kept verbatim (including explicit zeros).\n\n\n\n\n\n","title":"OpSum.sparse_from_dict"},{"category":"method","location":"reference/#OpSum.spin-Tuple{TensorKit.ElementarySpace}","page":"Reference","text":"spin(V::ElementarySpace)\n\nThe SU(2) rank-1 vector operator on a single-sector SU(2) space V, normalized so spin(V)[i] · spin(V)[j] densifies to the Cartesian Sˣ⊗Sˣ + Sʸ⊗Sʸ + Sᶻ⊗Sᶻ. Concretely spin(V) = √(s(s+1)(2s+1)) · IrrepOperator(SU2Irrep(1), 1) (the reduced matrix element).\n\nMemoised per space, so calling it inside a term loop is a dictionary lookup. See also spin_ops for the U(1)-graded (Sp, Sm, Sz) form.\n\n\n\n\n\n","title":"OpSum.spin"},{"category":"method","location":"reference/#OpSum.spin_ops-Union{Tuple{I}, Tuple{TensorKit.ElementarySpace, AbstractVector{I}}} where I<:TensorKitSectors.Sector","page":"Reference","text":"spin_ops(V::ElementarySpace, sectors::AbstractVector) -> NamedTuple\nspin_ops(V::ElementarySpace, up, dn)\n\n(; Sp, Sm, Sz) of a U(1)-graded spin-s site, where sectors lists the 2s+1 sectors of V in descending magnetic quantum number (m = s, s-1, …, -s):\n\nSᶻ = Σ_m m |m⟩⟨m|,    S⁺ = Σ_m √(s(s+1) - m(m+1)) |m+1⟩⟨m|,    S⁻ = (S⁺)ᵀ\n\nso that couple(Sz[i], Sz[j]) + (couple(Sp[i], Sm[j]) + couple(Sm[i], Sp[j])) / 2 is the Cartesian S⃗ᵢ · S⃗ⱼ — the same operator dot(spin(V)[i], spin(V)[j]) builds under SU(2).\n\nThe m values come from the order of sectors, never from the sector labels. A Vect[U₁] spin site is just as often labelled by particle number as by m (Rep[U₁](0 => 1, 1 => 1) for spin-½ is the common spelling), so inferring them would be a silent guess. The two-sector case takes its sectors directly: spin_ops(V, up, dn).\n\nEach sector of V must be one-dimensional with multiplicity one — what matrixunit needs.\n\nSee also spin for the SU(2) irreducible-tensor form, and fermion_ops.\n\n\n\n\n\n","title":"OpSum.spin_ops"},{"category":"method","location":"reference/#OpSum.storedpairs-Tuple{SparseArrays.SparseMatrixCSC}","page":"Reference","text":"storedpairs(A::SparseMatrixCSC)\n\nIterate the stored entries of A as CartesianIndex(row, col) => value pairs, in column-major order. Unlike pairs(A) this skips structurally absent entries, which matters here because the element type is a symbolic operator rather than a number.\n\n\n\n\n\n","title":"OpSum.storedpairs"},{"category":"method","location":"reference/#OpSum.total-Union{Tuple{OpSum.Term{I}}, Tuple{I}} where I","page":"Reference","text":"total(t::Term) -> Sector\n\nThe total charge t couples to: the running bond charge after its last active site, or the unit sector for the identity term.\n\n\n\n\n\n","title":"OpSum.total"},{"category":"method","location":"reference/#OpSum.tree-Union{Tuple{OpSum.Term{I}}, Tuple{I}} where I","page":"Reference","text":"tree(t::Term) -> FusionTree\n\nThe left-nested (caterpillar) coupling tree of t, rebuilt from its stored running bond charges and vertex labels. Derived, not stored: for a caterpillar the two are the same information (irrepkey.jl).\n\n\n\n\n\n","title":"OpSum.tree"},{"category":"method","location":"reference/#OpSum.vertexlabels-Union{Tuple{TensorKit.FusionTree{I, K}}, Tuple{K}, Tuple{I}} where {I, K}","page":"Reference","text":"vertexlabels(f::FusionTree{I,K})\n\nFusion-vertex multiplicity labels, one per active position (1 at position 1, f.vertices[k-1] after).\n\n\n\n\n\n","title":"OpSum.vertexlabels"},{"category":"type","location":"reference/#OpSum.IrrepTensorOperators.IrrepOperator","page":"Reference","text":"IrrepOperator{I<:Sector} <: OperatorBasis\n\nLocal irreducible tensor operator (ITO) alphabet element, labelled by an operator charge c::I (the coupled leg) and a canonical integer n selecting which operator of that charge is meant. The element is interpreted relative to a physical space V which is supplied by context (like the sites/axes argument of instantiate), not stored in the struct.\n\nMaterializes (via instantiate) to a TensorKit TensorMap\n\nO_{c,n} :  V  ←  V ⊗ Vect[I](c => 1)\n\ni.e. an operator V←V carrying a dangling charge-c leg of degeneracy 1.\n\nThe sentinel index n = 0 (with c = unit(I)) is reserved for the Phase-3 pass-through identity (see passthrough): it is not an enumerated alphabet letter (instances yields n ≥ 1) and instantiates to the structural identity id(V).\n\n\n\n\n\n","title":"OpSum.IrrepTensorOperators.IrrepOperator"},{"category":"method","location":"reference/#Base.instances-Tuple{Type{<:OpSum.IrrepTensorOperators.IrrepOperator}, TensorKit.ElementarySpace}","page":"Reference","text":"instances(::Type{<:IrrepOperator}, V::ElementarySpace)\n\nReturn the full ITO alphabet spanning End(V): all (c, n) for every charge c appearing in fuse(V ⊗ V'), with the complete n range 1:dim(fuse(V ⊗ V'), c) (canonical block ordering).\n\n\n\n\n\n","title":"Base.instances"},{"category":"method","location":"reference/#OpSum.instantiate-Union{Tuple{I}, Tuple{OpSum.IrrepTensorOperators.IrrepOperator{I}, TensorKit.ElementarySpace}} where I<:TensorKitSectors.Sector","page":"Reference","text":"instantiate(op::IrrepOperator, V::ElementarySpace)\n\nMaterialize the canonical ITO TensorMap O_{c,n} : V ← V ⊗ Vect[I](c => 1).\n\nThe single nonzero reduced entry (selected by the canonical n-ordering: the n-th scalar over blocks(O)) is set to 1 / sqrt(dim(s)), where s is that entry's coupled/block sector. This normalizes the alphabet to be orthonormal under TensorKit's qdim-weighted inner (so inner(O, O) = dim(s)·|entry|² = 1).\n\n\n\n\n\n","title":"OpSum.instantiate"},{"category":"section","location":"examples/multibody/#Multi-body-interactions","page":"Multi-body interactions","text":"Everything so far has been two-body. couple is not limited to pairs: nesting it builds a caterpillar fusion tree, so three- and four-body SU(2)-invariant interactions are expressible directly, with the intermediate fusion channels as explicit physical labels.\n\nThis page shows how those channels work, which ones exist, and what they cost in bond dimension.\n\nusing OpSum: OpSum\ninclude(joinpath(pkgdir(OpSum), \"examples\", \"common.jl\"))\n\nV = SU2Space(1 // 2 => 1)\nS = spin(V)","title":"Multi-body interactions"},{"category":"section","location":"examples/multibody/#Three-body-terms-and-the-channel-constraint","page":"Multi-body interactions","text":"A three-body scalar built from three rank-1 operators is\n\nleft( vecS_1 times vecS_2 right) cdot vecS_3\n\nand in the caterpillar basis it is couple(couple(S[1], S[2]; to = j₁₂), S[3]; to = 0). The intermediate label j_12 is a genuine degree of freedom, but it is not free: the last inner line must be able to fuse with the final operator charge to reach the total charge. Since each vecS carries charge 1, reaching a singlet forces j_12 = 1:\n\nfor j12 in 0:2\n    try\n        H = opsum(\n            fill(V, 3),\n            couple(couple(S[1], S[2]; to = SU2Irrep(j12)), S[3]; to = SU2Irrep(0))\n        )\n        r = build(\"K=3 j12=$j12\", H; quiet = true)\n        println(\"  j12=$j12  valid    D_dense=$(r.Ddense)  lossless=$(islossless(H))\")\n    catch e\n        println(\"  j12=$j12  rejected: \", first(sprint(showerror, e), 72))\n    end\nend\n\nThe rejected cases are not a limitation but a consistency check: those operators do not exist as SU(2) scalars.","title":"Three-body terms and the channel constraint"},{"category":"section","location":"examples/multibody/#Four-body-terms","page":"Multi-body interactions","text":"With four rank-1 operators there are two inner lines, (j_12 j_123). The same reasoning forces j_123 = 1, leaving exactly three singlet channels:\n\nchannels = Tuple{Int, Int}[]\nfor j12 in 0:2, j123 in 0:3\n    try\n        H = opsum(\n            fill(V, 4),\n            couple(\n                couple(couple(S[1], S[2]; to = SU2Irrep(j12)), S[3]; to = SU2Irrep(j123)),\n                S[4]; to = SU2Irrep(0)\n            )\n        )\n        r = build(\"k4\", H; quiet = true)\n        push!(channels, (j12, j123))\n        println(\"  (j12=$j12, j123=$j123)  valid    D_dense=$(r.Ddense)\")\n    catch\n        nothing\n    end\nend\nchannels\n\nThese three are not merely distinct labels — they are mutually orthogonal operators, i.e. a complete orthogonal basis for four-body SU(2)-invariant interactions on four ordered sites:\n\nlet sites4 = fill(V, 4)\n    ops = [\n        instantiate(\n                couple(\n                    couple(couple(S[1], S[2]; to = SU2Irrep(a)), S[3]; to = SU2Irrep(b)),\n                    S[4]; to = SU2Irrep(0)\n                ),\n                sites4\n            ) for (a, b) in channels\n    ]\n    [\n        round(real(dot(ops[i], ops[j]) / (norm(ops[i]) * norm(ops[j]))); digits = 10)\n            for i in eachindex(ops), j in eachindex(ops)\n    ]\nend","title":"Four-body terms"},{"category":"section","location":"examples/multibody/#A-note-on-ring-exchange","page":"Multi-body interactions","text":"The caterpillar tree fixes the site order, so the j_12 channel corresponds to the pairing (vecS_1cdotvecS_2)(vecS_3cdotvecS_4) — the j₁₂ = 0 channel is exactly that product. The other two pairings, (vecS_1cdotvecS_3)(vecS_2cdotvecS_4) and (vecS_1cdotvecS_4)(vecS_2cdotvecS_3), are linear combinations of all three channels, with coefficients given by recoupling (F-move) symbols.\n\nSo a specific physical ring-exchange operator is a particular linear combination of the three channels rather than a single one. Since every combination has the same MPO bond structure, the cost story below is unaffected by which one you pick.","title":"A note on ring exchange"},{"category":"section","location":"examples/multibody/#A-four-body-model-on-a-ladder","page":"Multi-body interactions","text":"To see multi-body terms in a Hamiltonian rather than in isolation, add plaquette terms to a two-leg ladder. With column-major ordering the four sites of the plaquette at column x are the consecutive indices 2x-1 ldots 2x+2, so the j₁₂ = 0 channel couples rung x to rung x+1.\n\nfunction plaquette_ladder(Lx; J = 1.0, K = 0.3, j12 = 0)\n    bonds = ladder_bonds(Lx, 2)\n    two_body = [J * dot(S[i], S[j]) for (i, j) in bonds]\n    plaquettes = [\n        K * couple(\n                couple(couple(S[4x - 3], S[4x - 2]; to = SU2Irrep(j12)), S[4x - 1]; to = SU2Irrep(1)),\n                S[4x]; to = SU2Irrep(0)\n            ) for x in 1:(div(Lx, 2))\n    ]\n    return opsum(fill(V, 2Lx), two_body, plaquettes)\nend\n\nH4 = plaquette_ladder(6)\nres4 = build(\"ladder + plaquettes\", H4)\nislossless(H4)\n\nCompare against the pure two-body ladder to isolate what the four-body terms cost:\n\nlet Lx = 6\n    two_only = opsum(fill(V, 2Lx), (dot(S[i], S[j]) for (i, j) in ladder_bonds(Lx, 2)))\n    two = build(\"ladder only\", two_only; quiet = true)\n    println(\"  two-body only:        D=$(two.D)  D_dense=$(two.Ddense)\")\n    println(\"  with plaquettes:      D=$(res4.D)  D_dense=$(res4.Ddense)\")\nend\n\nAnd the bond dimension still saturates with system size:\n\nfor Lx in (4, 6, 8, 12)\n    r = build(\"plaq\", plaquette_ladder(Lx); quiet = true)\n    println(\"  Lx=$(rpad(Lx, 3)) N=$(rpad(2Lx, 3))  D=$(rpad(r.D, 3))  D_dense=$(r.Ddense)\")\nend","title":"A four-body model on a ladder"},{"category":"section","location":"examples/multibody/#Scaling","page":"Multi-body interactions","text":"julia --project=benchmark scripts/plot_benchmarks.jl --run --sweep full\n\n(Image: Bond dimension and construction time versus system size)\n\n\n\nThis page was generated using Literate.jl.","title":"Scaling"},{"category":"section","location":"#OpSum.jl","page":"Home","text":"(Image: Stable) (Image: Dev) (Image: Build Status) (Image: Coverage) (Image: Code Style: Runic) (Image: Aqua)\n\nOpSum.jl converts sums of symmetric quantum operators — Hamiltonians — into exact, symmetry-reduced matrix product operators. The pipeline is: symbolic term algebra → flat term list → per-bond-sector compression → reduced MPO tensors.\n\nTwo things distinguish it. The compression is lossless: the resulting MPO reproduces every term of the input exactly, rather than being an approximation with a truncation threshold. And the symmetry is carried all the way through, so the bond indices are irreducible-representation labels and the MPO tensors come out as symmetric TensorMaps.\n\nThe compression algorithms are inspired by ITensorMPOConstruction.jl; the contribution here is folding non-abelian symmetry into them and making them work on TensorKit objects; see the Acknowledgements at the bottom of this page.","title":"OpSum.jl"},{"category":"section","location":"#Installation","page":"Home","text":"This package is not yet registered. It can be installed directly from GitHub:\n\njulia> using Pkg: Pkg\n\njulia> Pkg.add(url=\"https://github.com/lkdvos/OpSum.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"The SU(2) Heisenberg chain — nearest-neighbour spin-spin coupling on a spin-½ chain — end to end:\n\nusing OpSum\nusing OpSum: irrep_mpo, mpo_terms, spin\nusing TensorKit\nusing LinearAlgebra: dot\n\nV = SU2Space(1 // 2 => 1)          # one spin-½ per site\nN = 8\nsites = fill(V, N)\n\nS = spin(V)                        # the SU(2) rank-1 vector operator\nH = opsum(sites, (dot(S[i], S[i + 1]) for i in 1:(N - 1)))   # terms + the lattice they live on\n\nWs, sectors = irrep_mpo(H)         # reduced bond matrices + per-bond charge sectors\n\nThe bond dimension: sectors[b] lists the irrep labels on the bond to the right of site b, so its length is the number of symmetry-resolved indices, and the quantum-dimension-weighted sum is what a symmetry-agnostic MPO would need.\n\nbulk = 4\n(reduced = length(sectors[bulk]), dense_equivalent = sum(dim(c) for c in sectors[bulk]))\n\nThree multiplets — identity-in, identity-out, and one open spin-1 channel — where a dense MPO needs five states.\n\nThe compression is exact, which mpo_terms verifies by reconstructing the original term sum:\n\nback = mpo_terms(Ws, sectors, sites)\nback ≈ H","title":"Quick start"},{"category":"section","location":"#Examples","page":"Home","text":"The example pages build a range of models and measure how the bond dimension responds:\n\nShared utilities — naming alphabet letters, coupling composite operators, lattice geometry, and the verification helpers.\nSpin chains — Heisenberg, XXZ and J_1–J_2: how symmetry and interaction range set the bond dimension.\nLong-range interactions — Haldane–Shastry and power laws, the one family whose bond dimension grows with N.\nLadders and cylinders — quasi-2D geometries, where the bond dimension tracks the circumference and not the length.\nMulti-body interactions — three- and four-body couplings, and the fusion channels that label them.\nFermions — free chains and the Fermi–Hubbard model, with no Jordan–Wigner strings anywhere.\n\nConstruction time and bond dimension across system size are measured by the benchmark harness in benchmark/ and plotted with scripts/plot_benchmarks.jl.","title":"Examples"},{"category":"section","location":"#Acknowledgements","page":"Home","text":"The MPO construction here is inspired by ITensorMPOConstruction.jl (MIT licensed, © 2024 Ben Corbett and contributors), which demonstrated that exact minimal-bond-dimension MPOs can be built by bipartite-graph compression. OpSum.jl takes those algorithms and folds in non-abelian symmetry, so that bond indices carry irreducible-representation labels and the MPO tensors come out as TensorKit TensorMaps.\n\nThe underlying bipartite-graph / minimum-vertex-cover method is due to J. Ren, W. Li, T. Jiang and Z. Shuai, A general automatic method for optimal construction of matrix product operators using bipartite graph theory, J. Chem. Phys. 153, 084118 (2020).\n\nITensorMPOConstruction.jl asks to be cited as B. Corbett and A. Miyake, Scaling up the transcorrelated density matrix renormalization group, Phys. Rev. B 112, 165120 (2025).\n\n\n\nThis page was generated using Literate.jl.","title":"Acknowledgements"},{"category":"section","location":"examples/fermions/#Fermions:-free-chains-and-the-Hubbard-model","page":"Fermions: free chains and the Hubbard model","text":"Fermionic models need no special support here: TensorKit's graded (fermionic) sectors carry the anticommutation through the braiding, and the ITO machinery inherits it. In particular no Jordan–Wigner strings appear anywhere — the odd-parity charge flowing along the virtual bond does that bookkeeping for you.\n\nThere is exactly one thing to get right — the anticommutation sign — and this page is built around it, including who supplies it.\n\nusing OpSum: OpSum\ninclude(joinpath(pkgdir(OpSum), \"examples\", \"common.jl\"))","title":"Fermions: free chains and the Hubbard model"},{"category":"section","location":"examples/fermions/#A-fermionic-mode","page":"Fermions: free chains and the Hubbard model","text":"FermionNumber is U1Irrep ⊠ FermionParity: particle number together with the parity that makes the braiding fermionic. A single spinless mode is empty or occupied:\n\nV = Vect[FermionNumber](0 => 1, 1 => 1)\nvac, occ = FermionNumber(0), FermionNumber(1)\n\nThe operators are matrix units, derived rather than hard-coded. fermion_ops bundles the three of them so the convention has one definition:\n\nF = fermion_ops(V)\nc, cd, nh = F.c, F.cd, F.n     # ``c``, ``c^\\dagger``, ``\\hat{n}``\n\nBraidingStyle(sectortype(V))","title":"A fermionic mode"},{"category":"section","location":"examples/fermions/#The-sign,-and-who-supplies-it","page":"Fermions: free chains and the Hubbard model","text":"H = -t sum_i left( c^dagger_i c_i+1 + c^dagger_i+1 c_i right)\n\nA term is stored in site order, so couple(cd[i + 1], c[i]) cannot be kept as written: its two legs have to be swapped, and swapping two odd-parity legs costs\n\nc^dagger_i+1 c_i = - c_i c^dagger_i+1\n\nUnder an abelian symmetry — every fermionic sector is one — couple does that swap and inserts the phase itself, so the Hamiltonian is written the way it is printed above. H' produces the same partner from the first half.\n\nfunction hopping(N; t = 1.0)\n    return opsum(\n        fill(V, N),\n        (-t * (couple(cd[i], c[i + 1]) + couple(cd[i + 1], c[i])) for i in 1:(N - 1))\n    )\nend\n\nN = 8\nH = hopping(N)\n\nforward = opsum(fill(V, N), (-1.0 * couple(cd[i], c[i + 1]) for i in 1:(N - 1)))\nH ≈ forward + forward'\n\nNothing stops you from supplying a sign by hand, and then it is yours to get right: written in storage order the h.c. partner of c^dagger_i c_i+1 is -c_i c^dagger_i+1, and a + there is not a subtle error — the operator simply stops being hermitian, which is a cheap and very sharp check.\n\nbyhand(N; t = 1.0, sign = -1.0) = opsum(\n    fill(V, N),\n    (-t * (couple(cd[i], c[i + 1]) + sign * couple(c[i], cd[i + 1])) for i in 1:(N - 1))\n)\n\nH ≈ byhand(N)\n\n(correct = hermiticity_error(H), wrong = hermiticity_error(byhand(N; sign = +1.0)))","title":"The sign, and who supplies it"},{"category":"section","location":"examples/fermions/#Verification-against-the-exact-spectrum","page":"Fermions: free chains and the Hubbard model","text":"For an open chain the single-particle energies are varepsilon_k = -2tcos(kpi(N+1)) and the many-body spectrum is every subset sum of them. Note that we compare eigenvalues, computed block by block — for fermionic sectors convert(Array, t) is not a well-defined operation (TensorKit will warn that it does not preserve the categorical structure), so the dense-matrix oracle used elsewhere is simply unavailable here.\n\nfunction free_fermion_spectrum(N; t = 1.0)\n    ε = [-2t * cos(k * π / (N + 1)) for k in 1:N]\n    return sort(\n        [\n            sum(ε[k] for k in 1:N if (m >> (k - 1)) & 1 == 1; init = 0.0)\n                for m in 0:(2^N - 1)\n        ]\n    )\nend\n\nspectrum(H) ≈ free_fermion_spectrum(N)\n\nThe MPO is lossless, and its bond dimension is independent of N just as for the spin chains:\n\nislossless(H)\n\nres_free = build(\"free fermions\", H)","title":"Verification against the exact spectrum"},{"category":"section","location":"examples/fermions/#Adding-a-density–density-interaction","page":"Fermions: free chains and the Hubbard model","text":"H = -t sum_i left( c^dagger_i c_i+1 + mathrmhc right) + V sum_i hatn_i hatn_i+1\n\nThe t–V model. Both hatn factors are single letters, so this is an ordinary two-site term.\n\nfunction tV_chain(N; t = 1.0, Vint = 2.0)\n    return append!(\n        hopping(N; t),\n        (Vint * couple(nh[i], nh[i + 1]) for i in 1:(N - 1))\n    )\nend\n\nH_tV = tV_chain(N)\nres_tV = build(\"t-V chain\", H_tV)\nislossless(H_tV)","title":"Adding a density–density interaction"},{"category":"section","location":"examples/fermions/#The-Fermi–Hubbard-model","page":"Fermions: free chains and the Hubbard model","text":"H = -t sum_isigma left( c^dagger_isigma c_i+1sigma + mathrmhc right)\n    + U sum_i hatn_iuparrow hatn_idownarrow\n\nA spinful site has four states, and there c^dagger_uparrow is unavoidably a sum of two alphabet letters (it maps both 0rangle to uparrowrangle and downarrowrangle to uparrowdownarrowrangle) whose relative sign is a basis convention.\n\nThe standard way to sidestep that entirely is to give each spin-orbital its own site. Then every operator is a single letter again, and the on-site interaction hatn_iuparrowhatn_idownarrow becomes an ordinary two-site term between the two orbitals of the same physical site. We interleave them, (isigma) mapsto 2(i-1) + sigma, so each physical site is contiguous.\n\norbital(i, σ) = 2 * (i - 1) + σ    # σ = 1 for ↑, 2 for ↓\n\nfunction hubbard(Nsites; t = 1.0, U = 4.0)\n    hop = [\n        -t * (\n                couple(cd[orbital(i, σ)], c[orbital(i + 1, σ)]) +\n                couple(cd[orbital(i + 1, σ)], c[orbital(i, σ)])\n            )\n            for i in 1:(Nsites - 1) for σ in 1:2\n    ]\n    int = [\n        U * couple(nh[orbital(i, 1)], nh[orbital(i, 2)])\n            for i in 1:Nsites\n    ]\n    return opsum(fill(V, 2Nsites), hop, int)\nend\n\nNsites = 6\nH_hub = hubbard(Nsites)\nres_hub = build(\"Hubbard 1D\", H_hub)\nislossless(H_hub)","title":"The Fermi–Hubbard model"},{"category":"section","location":"examples/fermions/#Checking-it-against-two-exactly-solvable-limits","page":"Fermions: free chains and the Hubbard model","text":"At U = 0 the model is two independent species of free fermions, so the spectrum is every subset sum over the two copies of the single-particle levels:\n\nlet Ns = 3\n    H0 = hubbard(Ns; U = 0.0)\n    ε = vcat(\n        [-2 * cos(k * π / (Ns + 1)) for k in 1:Ns],\n        [-2 * cos(k * π / (Ns + 1)) for k in 1:Ns],\n    )\n    exact = sort(\n        [\n            sum(ε[k] for k in 1:(2Ns) if (m >> (k - 1)) & 1 == 1; init = 0.0)\n                for m in 0:(2^(2Ns) - 1)\n        ]\n    )\n    spectrum(H0) ≈ exact\nend\n\nAt t = 0 nothing moves, so the energy just counts doubly-occupied sites in units of U:\n\nlet Ns = 3, U = 4.0\n    Ht = hubbard(Ns; t = 0.0, U)\n    sort(unique(round.(spectrum(Ht); digits = 8)))\nend","title":"Checking it against two exactly solvable limits"},{"category":"section","location":"examples/fermions/#Bond-dimension","page":"Fermions: free chains and the Hubbard model","text":"As for every other local model, the Hubbard bond dimension saturates:\n\nfor Ns in (4, 6, 8, 12)\n    r = build(\"hub\", hubbard(Ns); quiet = true)\n    println(\"  sites=$(rpad(Ns, 3)) orbitals=$(rpad(2Ns, 3))  D=$(rpad(r.D, 3))  D_dense=$(r.Ddense)\")\nend\n\nThe spin-orbital encoding leaves a visible fingerprint on the profile of the bond dimension: it alternates between consecutive bonds, because a bond cutting within a physical site (between its ↑ and ↓ orbitals) carries the half-finished on-site interaction, while a bond cutting between physical sites does not.\n\nlet Ns = 8\n    _, secs = irrep_mpo(hubbard(Ns))\n    [densedim(secs, b) for b in eachindex(secs)]\nend","title":"Bond dimension"},{"category":"section","location":"examples/fermions/#Hubbard-on-a-cylinder","page":"Fermions: free chains and the Hubbard model","text":"Nothing about the construction is one-dimensional: swapping the chain's bond list for a cylinder's gives the quasi-2D Hubbard model. The physical sites form the cylinder, and each carries its two spin-orbitals.\n\nfunction hubbard_cylinder(Lx, Ly; t = 1.0, U = 4.0)\n    Nsites = Lx * Ly\n    hop = [\n        -t * (\n                couple(cd[orbital(i, σ)], c[orbital(j, σ)]) -\n                couple(c[orbital(i, σ)], cd[orbital(j, σ)])\n            )\n            for (i, j) in cylinder_bonds(Lx, Ly) for σ in 1:2\n    ]\n    int = [U * couple(nh[orbital(i, 1)], nh[orbital(i, 2)]) for i in 1:Nsites]\n    return opsum(fill(V, 2Nsites), hop, int)\nend\n\nfor Lx in (2, 4, 6, 8)\n    r = build(\"hub cyl\", hubbard_cylinder(Lx, 4); quiet = true)\n    println(\"  $(Lx)x4 cylinder: sites=$(rpad(4Lx, 3)) orbitals=$(rpad(8Lx, 3))  D_dense=$(r.Ddense)\")\nend\n\nThe bond dimension saturates at 19 once the cylinder is longer than two columns, and stays there out to hundreds of sites — the same length-independence seen for the spin cylinders.","title":"Hubbard on a cylinder"},{"category":"section","location":"examples/fermions/#Scaling","page":"Fermions: free chains and the Hubbard model","text":"julia --project=benchmark scripts/plot_benchmarks.jl --run --sweep full\n\n(Image: Bond dimension and construction time versus system size)\n\n\n\nThis page was generated using Literate.jl.","title":"Scaling"}]
}
